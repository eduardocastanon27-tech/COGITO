# Cogito — Strategy Playbook (what WORKS)

The other half of the brain. The lessons ledger remembers what FAILED (scars);
this file remembers what WORKED (strategies) — with counters proving it keeps
working. Research basis: ACE (arXiv 2510.04618) — a smaller model with an evolved
strategy playbook matched a GPT-4.1-based production agent; the lift came from
accumulating strategies, not just avoiding mistakes.

**Delta discipline (never break it):**
- NEW strategy that visibly worked → APPEND one bullet with a fresh `[P###]` id
  and zeroed counters. Never rewrite the file wholesale (context collapse).
- A loaded bullet HELPED this session → increment its `[helpful:N]` in place.
- A loaded bullet MISLED you → increment its `[harmful:N]` in place.
- Curation (dedup/merge/demote) happens ONLY at consolidation time
  (skill: cogito-consolidate); a demoted bullet MOVES to PLAYBOOK-ARCHIVE.md
  (harmful > helpful = demote candidate), never deleted.

**Format:** `- [P###][#tag][helpful:N][harmful:N] imperative strategy {via:origin}`

## Verification & done-claims
- [P001][#verify][helpful:1][harmful:0] Pick the deterministic done-check BEFORE starting (a command, an HTTP status, a re-read from disk, a test) and paste its output with the claim — "done" without pasted evidence is not done. {via:seed 2026-07-03, distilled from ledger #verify rules}
- [P002][#verify][helpful:0][harmful:0] For web work: prove by logged-out rendered pixels + the asset's own HTTP status + a clean console — never a green build or an owner-session view. {via:seed 2026-07-03}
- [P003][#verify][helpful:8][harmful:0] When a gate/scanner/agent claim drives an irreversible action, re-verify that specific claim with a grounded check first (cmp, diff -rq, curl, pgrep -af, unzip -l) — subagent adjectives are hypotheses. {via:seed 2026-07-03}
## Git & deploy
- [P004][#git][helpful:0][harmful:0] Before merging or rescuing an old branch, diff its FILES against the target (md5 per file + git diff target..branch) — file-level diff is the only content-truth; commit ancestry lies after squash-merges. {via:seed 2026-07-03}
- [P005][#deploy][helpful:0][harmful:0] Before retiring/deleting any deployment or endpoint, enumerate its inbound consumers first (grep sibling repos for its URL/env refs, check API routes) — a public endpoint is a production dependency. {via:seed 2026-07-03}
- [P006][#git][helpful:0][harmful:0] Set the faceless identity as LOCAL git config before the first history-writing op in any cogito-family repo — it covers commit, merge, and rebase at once. {via:seed 2026-07-03}
## Memory & sessions
- [P007][#memory][helpful:1][harmful:0] Two live copies of one file: diff BOTH directions, merge to the superset, write the same merged result to both — never cp one over the other. {via:seed 2026-07-03}
- [P008][#memory][helpful:0][harmful:0] Big input or long task: chunk it and keep a compact running index; re-expand only the chunk the current step needs (working memory = buffer × compression × retrieval). {via:seed 2026-07-03}
## Process
- [P009][#process][helpful:0][harmful:0] State the key assumption + confidence before non-trivial work; below ~80%, ask ONE clarifying question instead of guessing. {via:seed 2026-07-03}
- [P010][#process][helpful:0][harmful:0] Three failed honest tries → stop and surface the blocker with HELP REQUESTED: <action> BECAUSE: <reason> — a precise ask is a success state, not a failure. {via:seed 2026-07-03}
- [P011][#process][helpful:0][harmful:0] Diagnose the LAYER before the logic: transport (egress/proxy/policy) impersonates app errors; a deterministic rejection is never retried, a policy gap is named to the user. {via:seed 2026-07-03}
- [P012][#shell][helpful:1][harmful:0] Stop a process by explicit PID (pgrep pattern, then kill "$PID") or a port check — and feed grep -q via herestring/file, never as the receiving end of a pipeline under pipefail. {via:seed 2026-07-03}
- [P013][#process][helpful:0][harmful:0] For single-file Alpine/browser apps, verify JS without a browser: extract the inline <script>, run it in node vm with mocked window/localStorage/AudioContext, then call the data factory and exercise each method path (keys present, state flows, mocked node graphs) — catches wiring typos a parse check misses. {via:learn 2026-07-03}
- [P016][#process][helpful:0][harmful:0] Proactive WhatsApp push to Eduardo = mirror the hermes-*-reminder pattern: a script importing send.py (POSTs to 127.0.0.1:3000/send) + a systemd USER service/timer pair (Type=oneshot, OnCalendar=<date>, Persistent=true), enable --now, verify with 'systemctl --user list-timers'. Timers, NOT cron (crond isn't running); test with a --dry flag. {via:learn 2026-07-04}
- [P017][#process][helpful:0][harmful:0] Web eyes+audit on this box: see_site.sh screenshots any URL via preinstalled /usr/bin/chromium headless (then READ the PNG); site_audit.py (stdlib) scores raw HTML. Markup misses layout breakage: always cross-check pixels. Wire keyed MCPs via bash -lc sourcing ~/.hermes/.env.keys (never print the secret); prove the KEY with a direct curl 200. {via:learn 2026-07-04}
- [P018][#process][helpful:0][harmful:0] Analyze an audio file's key/scale/tempo with zero heavy deps: ffmpeg decode to mono s16le -> pure-python Goertzel chroma per pitch class (correlate vs Krumhansl profiles for key/mode) + energy-envelope autocorrelation for BPM; cache per path+mtime. Fast enough (~4s/80s track) on a 2.78GB box where numpy/librosa aren't installed. {via:learn 2026-07-05}
- [P019][#process][helpful:0][harmful:0] For a headless browser page, prove init doesn't runtime-crash by eval-ing index.html's inline <script> under a minimal DOM+canvas+AudioContext shim and stepping ~40 frames; assert a live signal (foot shows 'gen N'). node --check only proves syntax. {via:learn 2026-07-06}
- [P020][#process][helpful:1][harmful:0] Long claude -p jobs from a Hermes/WhatsApp turn: launch fully detached (setsid nohup /full/path/claude -p ... > /tmp/log 2>&1 &) -- plain background children die when the per-turn session cycles. Gauge progress by project file mtimes/greps, not the log: claude -p buffers and the logfile stays 0 bytes until the end. {via:learn 2026-07-06}
- [P021][#dispatch][helpful:0][harmful:0] DECISION-COMPLETE HANDOFF: before dispatching Claude Code, write the task so the worker makes ZERO decisions of its own — pack the plan, exact file paths, repo conventions, and how to verify into the prompt (it can't see your context). Vague dispatch = the #1 cause of wrong output. {via:learn 2026-07-06}
- [P022][#dispatch][helpful:0][harmful:0] SUB-AGENT OUTPUT IS DATA, NOT A USER MESSAGE: a dispatched agent's final text returns to ME, not Eduardo. Extract the load-bearing lines and re-report in my own words; never forward a raw transcript. When running >1 agent in parallel, give each a DISJOINT write set (non-overlapping files) so they can't clobber each other. {via:learn 2026-07-06}
- [P023][#dispatch][helpful:2][harmful:0] TIER THE WORK + SCALE THE CHECK: answer quick questions on my own brain; only dispatch Claude Code for real project changes. Verification scales with blast radius (typo=none, shared contract=broad). If a change would touch >3 files or multiple subsystems, get a short plan back before it edits. 'How's it going?' = report and keep working, not stop. {via:learn 2026-07-06}
- [P024][#verify][helpful:0][harmful:0] when evaluating scaffolding/context lifts, expect modern models to ceiling the FAMOUS failure modes natively -- discrimination lives in project-, machine-, and owner-specific scenarios no training data carries {via:learn 2026-07-03; renumbered from canon P013 at 2026-07-07 superset merge}
- [P025][#process][helpful:0][harmful:0] Substantial multi-requirement build: council for the design (diverse lenses, verify load-bearing claims), then build in verifiable SLICES on a FEATURE branch (never main), proving each slice by a real outcome before the next -- incremental verify catches what a big-bang commit buries. {via:learn 2026-07-04; renumbered+tightened from canon P014 at 2026-07-07 superset merge}
- [P026][#process][helpful:0][harmful:0] Session died mid-run (spend limit/crash)? Recover it from the transcript: ~/.claude/projects/<proj>/<session-id>.jsonl survives even when the UI loses the session. Parse assistant text + tool_use inputs (Write/Edit old/new) + tool_results to reconstruct BOTH what was produced and which edits landed; workflow results also persist in subagents/workflows/<id>/journal.jsonl. {via:learn 2026-07-07}
- [P027][#process][helpful:0][harmful:0] Token-tuning a claude -p overseer stack: trim what rides EVERY turn first — skill frontmatter descriptions, always-injected system prompts (SOUL/registry), and memory indexes — because each byte there multiplies across every WhatsApp message; verify with the consumer (a real claude -p echo of the new description, the proxy's own concat logic vs its cap), not a file diff. {via:learn 2026-07-09}
- [P028][#process][helpful:0][harmful:0] Overseer quality lives in the DISPATCH PROMPT, not the overseer's head: bake the worker contract (TASK/CONTEXT/SCOPE/CHECKPOINT/VERIFY/REPORT-with-pasted-output) into a fill-in template in the dispatch skill, and make the overseer re-run VERIFY itself — a weak model follows a form far more reliably than a principle. {via:learn 2026-07-09}
- [P029][#process][helpful:0][harmful:0] For untrusted serialized input (share links, community DB rows), validate with the SAME whitelist at every boundary: the fast path (codegen) AND the import path (decode/load/draw) — a validator that only guards the optimizer still lets hostile data crash the interpreter loop. {via:learn 2026-07-12}
- [P030][#process][helpful:0][harmful:0] Generative/LLM-authored UI belongs somewhere ONLY when the interface is unknowable at build time (runtime-unknowability test): CRUD/admin schemas fail it, a tutor's per-misconception explainer passes it. Never render model-authored HTML/JS same-origin in a client product; sandboxed cross-origin iframe + CSP + a proxy/key-layer spend cap (not a frontend concern) are the gate. In BIHO specifically, claude/* auto-merges to main in seconds, so an experimental branch = instant production. {via:learn 2026-07-12}
- [P031][#process][helpful:0][harmful:0] To apply a video/demo to a stack on a weak box: extract keyframes with ffmpeg (-ss T -frames:v 1) and READ them directly — frames alone gave the full concept; transcription (whisper.cpp greedy -bo 1 -bs 1, run setsid-detached so it survives session cycles) is slow confirmation, not a blocker. The reusable product pattern from 'AI replies in HTML': render model-authored interactive widgets in a sandboxed iframe, let the user REFINE them in place by talking (/tweak), and let the agent decide to render proactively via an in-reply 'SHOW: <concept>' marker that costs no extra call and is parsed out of the spoken text. {via:learn 2026-07-12}
- [P032][#process][helpful:0][harmful:0] ADVERSARIAL PRIVACY TEST: when a filter guards private data, don't just test the happy path — craft the input that tries hardest to pull the protected content through (task words matching the private file's index line), FIRST verify the distinctive strings exist in the source (so zero-hits means something), then assert zero hits in the output plus a structural check (every emitted item carries the required tag). {via:learn 2026-07-16}
- [P033][#process][helpful:0][harmful:0] For cross-language 'independent re-derivation' proof: mirror the same seeded PRNG (mulberry32) with exact 32-bit int semantics in both languages and assert a byte-for-byte trade-log match — a shared spec with no shared code is far stronger evidence of correctness than same-language self-agreement. {via:learn 2026-07-17}
- [P034][#process][helpful:0][harmful:0] For a DSP/synthesis toy, make the falsifiable check a first-principles PREDICTION you then measure back (predict pitch = fs/(N+0.5), synthesise, recover it by autocorrelation to 0.000 cents) — and run the SAME kernel in Python, Node, and the actual browser so the verification proves portability, not just one runtime. {via:learn 2026-07-17}
- [P035][#process][helpful:0][harmful:0] For sandbox JS-toy verification, generate the numeric baseline from core.py into a baseline.json, then have drive.mjs read the LIVE page's own on-screen numbers over CDP and assert byte-identical match — but make the Python image the EXACT same resolution as the page, or resolution-dependent local stats diverge and fail spuriously. {via:learn 2026-07-17}
- [P036][#verify][helpful:1][harmful:0] [#process] ONE-SHOT A CORRECT-BY-CONSTRUCTION GENERATOR STACK: before writing, read ONE working triple (generator + its independent self-test branch + registry entry) and copy its shape; design every new problem with a PARSEABLE prompt so the independent gate can re-derive the answer; register + add the check branch in the SAME pass; then prove it green (self-test) AND build the real end artifact (worksheet) + hand-check a sample — the gate proves the key, the artifact proves the pipeline. {via:learn 2026-07-18}
- [P037][#verify][helpful:0][harmful:0] [#process] VERIFY THE BOTTLENECK BEFORE BUILDING THE FIX: when asked to 'make X faster', MEASURE where the time actually goes first (first-token vs full, trivial-prompt vs real-prompt, with/without a flag) — don't assume. For claude -p latency the villain was ~3s CLI cold-start (autoupdate/telemetry), not token streaming; streaming would have saved ~1.7s of the WRONG thing while adding real risk. A 3-sample median beats one noisy sample on a flaky box. {via:learn 2026-07-18}
- [P038][#process][helpful:0][harmful:0] [#ai-tutor] INSTANT HAPPY-PATH BEATS A FASTER MODEL: when a turn's outcome is already known deterministically (a grader says 'correct', the next item comes from a generator), answer it locally with NO model call — ~1ms vs ~3s — and spend the LLM only on the turns that need judgment (wrong/stuck/asking). Keep it from feeling robotic with a rotating, no-immediate-repeat phrasing pool. Cheaper, faster, AND less RAM/OOM pressure than any warm-process scheme. {via:learn 2026-07-18}
- [P039][#process][helpful:0][harmful:0] For a sandbox 'optimal algorithm' toy, prove optimality by BRUTE FORCE (enumerate every alternative on small inputs, confirm none beats it) rather than citing the theorem — 'we tried them all' is a grounded check; 'the theorem says so' is a proxy. {via:learn 2026-07-18}
- [P040][#process][helpful:0][harmful:0] When a verification suite proves an object's ALGEBRA but not the DYNAMICS its headline claim rests on, close the gap with falsifiable signatures anchored on an EXACT known constant (e.g. BTW stationary density 17/8) plus finite-size scaling plus a model-comparison test (Vuong power-law-vs-exponential) — and keep an honest caveat where the literature is genuinely contested (BTW multifractality) rather than pinning a fragile exponent. {via:learn 2026-07-18}
- [P041][#process][helpful:0][harmful:0] Tutor teaching DEPTH: the bottleneck is CONTENT not the engine — the TEACHER prompt already encodes strong pedagogy, but brain() taught each concept from the model's own head with no per-concept substrate. Fix: inject a per-concept TEACHING BRIEF (best intuition + sub-skill ladder + classic misconceptions each paired with the Socratic question that catches it) from briefs/<concept>.md into brain(), keyed to the current rung. Prove depth on ONE concept live, then roll the identical pattern across the ladder. {via:learn 2026-07-18}
- [P042][#process][helpful:0][harmful:0] AI-tutor: complete a template LADDER band before optimizing its cache — a per-concept parameterized template (instant, correct-by-construction, right for any numbers) strictly beats caching one fixed Fable blob keyed on a verbose description that rarely re-hits. Verify each new rung by probing find_template with BOTH the bare gradus id AND a worked example, then render the final step headless and LOOK. {via:learn 2026-07-19}
- [P043][#process][helpful:0][harmful:0] To find a genuinely NEW synthesis over a corpus that already has taxonomies, change the AXIS not the depth: after 'shared dynamics' (Atlas) and 'biological level' (Neuro-Spine), 'the exact constant each item hits' surfaced that 68 toys re-derive only ~8 math atoms — pick an orthogonal projection. {via:learn 2026-07-19}
- [P044][#process][helpful:0][harmful:0] AI-tutor templates: the build SMOKE TEST (_smoke() in templates.py) is the standing guard against the swallowed-build-exception class — it walks every REGISTRY rung, fires its matcher and calls build() with NO try/except, and asserts every rung has a probe. Run 'python3 templates.py' after ANY template edit; a format-string typo or a new rung with no probe fails LOUDLY there instead of silently routing to Fable. When you add a rung, add its _SMOKE line. {via:learn 2026-07-19}
- [P045][#process][helpful:0][harmful:0] For any generated index/gallery/manifest artifact, make it a PURE FUNCTION of a single source-of-truth file plus a generator script, and add a guard that REFUSES to emit when disk reality (dirs/files) diverges from the manifest — hand-edited indexes silently rot (gallery froze at 64 while 70 existed). Verify by re-parsing the emitted artifact independently and resolving every link/asset on disk. {via:learn 2026-07-19}
- [P046][#process][helpful:0][harmful:0] For a 'these N definitions are secretly the same number' toy (PageRank: stationary dist = leading eigenvector = random-walk limit), prove the identity three independent ways that CAN diverge — closed-form linear solve vs power iteration vs seeded Monte-Carlo — and make the RNG bit-for-bit shared across Python/JS so 'agreement' can't be a lucky coincidence. {via:learn 2026-07-20}
- [P047][#process][helpful:10][harmful:0] Single-file game polish: extract the <script type=module> body with awk and node --check it after EVERY edit, commit per item — 10 items landed with zero broken commits. {via:learn 2026-07-22}
- [P048][#process][helpful:0][harmful:0] Verify FPS-meter + adaptive-quality features with a headless chromium --headless=new swiftshader render: software WebGL is naturally ~5fps, so one screenshot proves the meter reads AND the auto-quality-drop fires (readout showed 'Q0') {via:learn 2026-07-22}
- [P049][#process][helpful:0][harmful:0] For long Hermes doer dispatches: keep the doer's job to EDIT + node --check + commit-per-item, and do the headless WebGL/pixel verification yourself afterward — software-GL screenshot capture is minutes-per-shot and OOM-flaky on the 2.78GB box, so a doer told to screenshot burns its whole window in the verify loop and commits nothing. {via:learn 2026-07-24}
- [P050][#process][helpful:0][harmful:0] Corpus health board: after N making/improving cycles, re-run health_check.py --write to refresh HEALTH.md — it stales silently (says 73/73 while corpus grew to 77) because nothing regenerates it on new-toy add; a fresh green board is the re-runnable proof the code (not just the index) still runs. {via:learn 2026-07-24}
- [P051][#process][helpful:0][harmful:0] Cogito's grading/feedback half rots silently (85% strategies never bumped) while capture works — audit the loop weekly and push the rot metric, don't trust that stored=applied {via:learn 2026-07-24}
- [P052][#process][helpful:0][harmful:0] System health/watchdog agents must be silent-unless-red AND false-positive-filtered — the first run of hermes_doctor.py caught 2 silently-failed units (curiosity, wcpredict) that plain --failed greps had missed; a queued send is durable success, not a red {via:learn 2026-07-24}
- [P053][#process][helpful:0][harmful:0] Wiring a live tool to a persistent store: keep the shared/global path byte-identical (env-gated opt-in, unset = old behavior), add write ops as new subcommands, and verify the OLD path still works before trusting the new one — the SessionStart hook depended on cogito-review.sh's global log. {via:learn 2026-07-13; recovered from stale repo playbook P034 during reconcile 2026-07-24}
- [P054][#process][helpful:0][harmful:0] For a generated-problem tool, make the printed prompt part of the contract: verify the answer key by (a) recomputing via an independent path and (b) asserting every number the answer depends on literally appears in the prompt text — the second half catches prompt/key drift that pure recomputation misses. {via:learn 2026-07-25}
- [P055][#process][helpful:0][harmful:0] For a generated answer key, make the check an INDEPENDENT path (closed form vs RK4 on the raw ODE) and enforce it structurally: the Problem object refuses to pass without a recheck closure, and requires every number the answer uses to appear in the printed prompt. Catches rounded-prompt/unrounded-key drift that no eyeball review finds. {via:learn 2026-07-25}
- [P056][#process][helpful:0][harmful:0] In a numerical verification suite, separate SEARCH steps from PRECISION steps: a bisection/root-find only needs its inner solver accurate enough not to flip the comparison, so cut its step count and iteration count hard (bracket tightly too) — pharmakon went 95s->40s with zero loss in the 853 comparisons or their tolerances. {via:learn 2026-07-25}
- [P057][#process][helpful:0][harmful:0] When a proxy shims a BUFFERED CLI (claude -p --output-format json) behind a STREAMING API, open the SSE stream and emit keepalive pings immediately instead of awaiting the whole run — a silent socket during a long generation trips the client's stale-stream watchdog and, if fallback providers are configured, silently swaps the brain mid-session. {via:learn 2026-07-25}
- [P058][#process][helpful:0][harmful:0] Reviving a timed-out build node: run the existing gate for a baseline count FIRST, then grep the analytic+numeric modules for the node's functions — a dispatch that died on wall-clock usually landed the math and lost only the wiring (generator, registry, curriculum flag, lesson, self-test section). Prove the new gate section actually bites by monkeypatching three named errors into the analytic module and re-running just that section. {via:learn 2026-07-26}
- [P059][#process][helpful:0][harmful:0] Building a verified-generator rung: derive the hidden patient parameter FROM the number you intend to print (set V so the steady-state trough equals the printed level exactly), so the student and the independent checker work from the identical number and rounding never splits them. {via:learn 2026-07-26}
- [P060][#process][helpful:0][harmful:0] For a new rung in a verified-generator stack: define the independent path's inputs OPERATIONALLY (fm = mass accounted to that pathway, not CL_i/sum) so the construction of the test body cannot import the identity under test; where a target must be hit exactly, use a fixed-point iteration whose UPDATE rule is a heuristic but whose CONVERGED point is defined only by marched measurements, and assert the residual before returning. {via:learn 2026-07-26}
- [P061][#process][helpful:1][harmful:0] Adding a rung to a verified generator ladder: trace ONE live rung end to end first (pk -> numeric -> generator -> registry -> curriculum -> lesson -> selftest), record the gate's baseline count, then build the independent path so it ROOT-FINDS over marched results whenever the problem states a ratio instead of raw parameters — that keeps the check from re-using the closed form it is meant to test. {via:learn 2026-07-26}
- [P062][#process][helpful:0][harmful:0] New rung on a verified-generator ladder: append the analytic section to pk.py first and smoke-test it in one line, THEN write the numeric twin from the raw mechanism (not the closed form's shape) and diff the two before touching any generator — the model is provably right before a single problem string exists, so every later failure is wiring or typography, never maths. {via:learn 2026-07-26}
- [P063][#process][helpful:0][harmful:0] New pharmakon rung in one pass: append pk closed forms + a numeric twin that marches the RAW ODE with per-route mass as integrator states, smoke-test each numeric fn against the closed form BEFORE writing the generator, then guard every while-True redraw with _distinct only — never _exact3 on a DERIVED value (it never rounds exactly and the loop spins forever). {via:learn 2026-07-26}
- [P064][#process][helpful:0][harmful:0] When adding a rung whose answer is a RATIO, build the independent body by root-finding its measurable properties (bisect binding capacity until measured fu matches, bisect CL_int until measured extraction ratio matches) rather than computing them from the closed form — then randomise every parameter the model says cannot reach the answer and assert that independence directly in the self-test. {via:learn 2026-07-26}
- [P065][#process][helpful:0][harmful:0] Before dispatching a prompt to another agent, pipe it through a mechanical lint (ste_check.py) — vague terms like 'properly'/'make sure'/'refactor' are decisions the writer never made, and the doer cannot ask. Exit 1 = rewrite before sending. {via:learn 2026-07-28}
- [P066][#process][helpful:0][harmful:0] Before swapping a live service to a new version, clone its venv and verify the NEW build in an isolated worktree first (imports, real-config parse against a COPY so a schema migration cannot rewrite the live file, targeted regression tests). Only then stop the service. {via:learn 2026-07-29}
- [P067][#process][helpful:0][harmful:0] Make silent degradation loud in two moves, not one: (1) remove the silent path itself (hermes fallback clear) so an outage hard-errors where a human is watching, (2) add a log-tailing watchdog + timer for the unattended runs nobody reads. Verify BOTH branches for real — run the healthy path and confirm exit 0 with no message, then force the failure (point the health probe at a dead port) and confirm the notification actually DELIVERED, checking the sender's failure queue rather than trusting the send call's return. {via:learn 2026-07-30}
- [P068][#process][helpful:0][harmful:0] When shipping a constant of CITED facts (edges, quotes, references), store the exact quoted substring next to the file:line and have the check script re-read the file and assert the substring is still on that line — an id-only check passes while the citation silently points at the wrong line. {via:learn 2026-07-30}
- [P069][#process][helpful:0][harmful:0] Retheming a canvas toy: patch the 2D context colour setters at serve time (flip HSL lightness, keep hue, snap the two extremes to the host palette) instead of editing N source files — CSS cannot reach canvas pixels, and a serve-time shim keeps the standalone originals intact and covers future toys for free {via:learn 2026-07-31}
- [P070][#process][helpful:0][harmful:0] Verifying a web UI on the 2.78GB box: reach for single-shot 'chromium --headless --screenshot' and '--dump-dom' FIRST (reliable, ~7/7 here) and keep the CDP websocket drive only for clicks; screenshot + DOM dump proves render, structure and deep links with no websocket to drop {via:learn 2026-07-31}
- [P071][#process][helpful:0][harmful:0] Brief-plus-thread work: write the brief, grep -n the verbatim link sentence for its REAL line number, then write the threads.py citation from that grep output — never from a guessed line; run check_threads.py and commit per item so a kill loses at most one brief. {via:learn 2026-07-31}
- [P072][#process][helpful:0][harmful:0] When you write an independent replica of a study, make the two copies DISAGREE-detectable: if the replica and the primary reach opposite verdicts, do not reconcile by tweaking one until it matches — find which is wrong with a known-answer control (feed both the exact case whose result is fixed by construction). On Plate II this exposed that the 'full range' window was defined three different ways across page, replica and driver, and that one of them rejects exact data. {via:learn 2026-08-01}
- [P073][#process][helpful:0][harmful:0] Auditing a live site: scroll the whole page before every fullPage screenshot — IntersectionObserver reveals and count-up animations stay at their initial state otherwise, and you will report 'stats show 0+' as a bug that does not exist. Verify a suspected bug against real user behaviour (scroll, then read the DOM) before writing it down. {via:learn 2026-08-01}
- [P074][#process][helpful:0][harmful:0] For a measured-vs-theory artifact, write the pure-Python replica FIRST as the prototype: it fixes the statistic, the thresholds and the fit windows against real numbers before a line of the page exists, and then doubles as the cross-language check (page vs replica agreed to 4e-8 on Plate III). {via:learn 2026-08-01}
- [P075][#process][helpful:0][harmful:0] For a hub page over verified artifacts: build it from a template plus a build.py that inlines assets as data URIs, so the page stays one self-contained file while the source stays editable — then verify links twice, once against the filesystem and once in a real browser dump. {via:learn 2026-08-01}
- [P076][#process][helpful:0][harmful:0] Before empirically sweeping a generated artefact for regressions, first diff it key-by-key against the pre-change version: keys that are byte-identical CANNOT have regressed, which collapses the blast radius to a handful and turns every later finding into either rebuild damage or a pre-existing bug you must not fix under a scope limit. {via:learn 2026-08-02}
- [P077][#process][helpful:0][harmful:0] When several problem families share one notation (coordinate pairs meaning vector-add, dot product, AND matrix rows), gate every family on its OPERATOR and make the independent checker dispatch on the same operator, specific readings before defaults — then prove no reading steals another's input by classifying a few thousand generated prompts back into families and checking the counts. {via:learn 2026-08-02}
- [P078][#process][helpful:0][harmful:0] Build a generated-content rung backwards from its checker: pick problem sizes small enough that an independent path can ENUMERATE the whole space by brute force, then the audit shares no formula with the generator and a wrong key cannot hide behind matching arithmetic. {via:learn 2026-08-02}
- [P079][#process][helpful:0][harmful:0] For a differential-equation (or any 'solve for a function') answer key, verify against the DEFINITION not the derivation: substitute the printed answer back into the printed equation (5-point numerical derivative vs the parsed right-hand side), then integrate the equation forward with RK4 from the printed initial condition and require the two curves to agree, then assert the limit cases numerically (y(0)=y0, y(t->inf)->a/k, 'the claimed t really hits its target'). Three paths, stdlib only, none of them the generator — and pick the sample window adaptively so a genuine pole (1/(1-kx)) is excluded rather than silently stepped over. {via:learn 2026-08-02}
- [P080][#process][helpful:0][harmful:0] When a batch gate stalls, do NOT assume slowness: check %CPU (spinning != blocked), then re-run the batch item-by-item under a per-item timeout so a hang reports as TIMEOUT instead of hiding as 'still running'. To locate the spin without a profiler, re-run the item under faulthandler.dump_traceback_later(N, exit=True) — it names the exact looping line. {via:learn 2026-08-02}
- [P081][#process][helpful:0][harmful:0] Stress-testing a live site: take BOTH a full-page captureBeyondViewport screenshot AND a real scrolled viewport shot — beyond-viewport capture misrenders anything driven by IntersectionObserver (count-up stats read 0+) or position:fixed (sticky bars float mid-page), so judge animated/fixed regions from the DOM + a true viewport shot and judge layout from the full-page one. Before filing ANY browser-reported resource failure, re-fetch the URL with curl: net::ERR_CERT_VERIFIER_CHANGED and ERR_FAILED are headless transients, and software-WebGL deprecation warnings are GPU-less-box artifacts, none are site defects. Budget a 'turned out to be FINE' section in the report — it is what separates a stress test from a list of guesses. {via:learn 2026-08-02}
- [P082][#process][helpful:0][harmful:0] When adding a new correct-by-construction generator rung, write the checker's re-derivation by a DIFFERENT mathematical route than the generator (variance via E[x^2]-mean^2 vs average-of-squared-deviations; probabilities by enumerating the sample space vs binomial coefficients). Same-route checkers only prove the code ran twice. {via:learn 2026-08-04}
- [P083][#process][helpful:0][harmful:0] New gradus rung = five artifacts or it is not shipped: generator, an INDEPENDENT re-derivation branch in verify_selftest.check_one (different algorithm, not the same formula), lesson md, tutor brief with a 'Link across' line naming a real node id, and a threads.py entry citing that line. Adding gen=True nodes to gradus/curriculum.py auto-extends the tutor ladder AND the Map (canvas_server reads mastery.curriculum.TIERS/NODES live) — so the wiring is free, but only the brief+thread makes it teachable. Verify in that order: 4000-problem gate, verify_packet, check_threads.py, then a real browser screenshot of the Map lane. {via:learn 2026-08-04}
- [P084][#process][helpful:0][harmful:0] To prove WHICH build is live for a single-file web game, compare the live byte count against 'git cat-file -s <commit>:path' across the last N commits — an exact match names the deployed commit with no dashboard, token or API access. {via:learn 2026-08-05}
- [P085][#process][helpful:0][harmful:0] For any command slower than a turn, launch it with keepwatch.sh --label X -- <cmd>: it self-detaches (setsid), streams to a log, and a bash EXIT/TERM trap texts the verdict via send.py — so completion reporting never depends on the agent being alive. {via:learn 2026-08-06}
- [P086][#process][helpful:0][harmful:0] To verify a browser tool that takes a FILE input, drive headless Chromium with CDP DOM.setFileInputFiles (DOM.enable -> getDocument -> querySelector -> setFileInputFiles) and assert on the rendered DOM; generate the fixture (e.g. a hand-written PDF) in pure Python so the test needs no libraries, and include a DEGENERATE fixture (a text-free PDF) to prove the honest-failure branch actually fires. {via:learn 2026-08-08}
- [P087][#process][helpful:0][harmful:0] Diagnose a ChromeOS-level symptom (cast/media/printer/AirPlay showing up unasked) from INSIDE Crostini: avahi-browse -rt reaches the real LAN, and the _googlecast._tcp TXT record answers it outright — st=0 idle, st=1 active, rs=<AppName> names the running app, fn= the room. Whose device it is falls straight out of the name+IP. {via:learn 2026-08-08}
