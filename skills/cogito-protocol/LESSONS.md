# Cogito — Lessons Ledger

The durable memory of the system. Each line is one earned lesson in the form:

    SYMPTOM -> ROOT CAUSE -> RULE

An entry is warranted whenever (skill §4b): the user corrects Claude; a bug's
root cause sits at a different layer than its symptom; a help request reveals a
missing tool/permission/context; or the same friction happens twice. Capture
prospectively — the moment it happens, not at the end. This file is the
write-path that makes the protocol compound; an empty ledger means the system
is relearning its scars every session.

> Verify on every session start that this file is reachable and survives a
> write (ephemeral containers silently revert). If it does not, say so
> immediately rather than shipping a ledger that looks alive but isn't.

**Format (from 2026-06-15):** prefer `[#tag] [I:1-10] SYMPTOM -> ROOT CAUSE -> RULE` so lessons can be retrieved by tag and ranked by importance; `#critical` or I ≥ 9 always load. When this list passes ~60 entries or after a severe lesson, run a consolidation pass (merge/supersede into fewer rules, archive raw lines with provenance, never delete). Rationale: docs/projects/06-cogito-upgrade-roadmap.md.

---

## Lessons

- [#memory][#process][#sector:infra] [I:7] Prose describes, hooks execute; "online" = durable + auto-loading (consolidates 2 prior merged rules) -> skill/CLAUDE.md text executes nothing, and memory living only in session files dies with the session -> wire the SessionStart hook + install the skill globally on day one, commit memory to a repo the user owns, and verify the ledger survives a write by re-reading it. {merged:2026-07-07}
- [#always][#critical][#git] [I:9] Faceless (x4 raw) -> defaults leak his handle/email -> local git config Cogito / 291881939+COGITO-SUM-cloude@users.noreply.github.com BEFORE the first history op in every cogito repo; scrub his handle from ALL public output; accept 'Unverified'.
- [#deploy][#verify] [I:7] Ship-truth (consolidates 5 raw, one a prior merge) -> deploy configs are intentions, echoed OKs lie (a dirty tree aborted a 'successful' merge), a fingerprint string that predates the change discriminates nothing, and deploy MCP tools only return CLI commands -> BEFORE: verify the deploy mechanism + auth + CLI/token exist; prefer git-based continuous deploy. AFTER: verify origin/main actually moved (git rev-parse) AND live content carries a token introduced ONLY by this change (confirmed absent from the old build). Vercel protection/deletion = dashboard-only (MCP is read/deploy-only; the private hub is the Vercel 'skills' project). {merged:2026-07-07}
- [#always][#critical][#comms] [I:9] Eduardo: non-technical, NOT a child -> adult-to-adult: plain words, short replies, analogy not jargon, do the tech myself; NO emojis/cheerleading/'baby steps' -- 'simpler' = less jargon, not talking down; re-read when a task feels complex.
- [#process][#teaching] [I:5] Teaching goals (consolidates 2 raw) -> the human is a learner too, and the adult-tuned skill does NOT transfer to a child -> treat user-growth as first-class (small, plain-language teaching woven into sessions); teaching his 5th-grade brother is NEW gently-scaffolded, decoding-first work that protects his dignity -- never reuse the adult register on a child. {merged:2026-07-07}
- [#always][#critical][#boundary] [I:10] Classifier denial (x3 raw) -> a block is a boundary, not an obstacle -> STOP: never reroute via files/env/sed/indirection, never infer consent from an indirect remark; present the exact change + rationale and get an explicit yes.
- [#web][#nextjs][#sector:web] [I:7] Installed-version truth (consolidates 2 raw) -> framework APIs drift from training data (this CMS runs a modified Next 16: async params, uncached route handlers) -> read node_modules/<framework>/dist/docs for the INSTALLED version and heed AGENTS.md deprecation notes before writing framework code. {merged:2026-07-07}
- [#web][#proj:idkwhatimdoing][#sector:web] [I:4] BIHO CMS conventions (consolidates 2 raw) -> the React compiler forbids impure calls (Date.now/random) during render, react-hooks/set-state-in-effect is an ERROR here, and themeable art rides one config -> compute time/random values in effects/handlers; inline-disable a legit effect-sync with a one-line reason; control every backdrop from src/lib/art.ts. {merged:2026-07-07}
- [#css][#web][#sector:web] [I:4] CSS layering/cascade scars (consolidates 5 raw) -> perceived brightness = image_opacity x (1 - scrim_alpha); non-positioned content paints above a z-0 backdrop; UNLAYERED globals beat Tailwind @layer utilities; tall cells shove markers off a shared axis; @theme palettes add utilities without removing defaults -> tune image opacity + scrim alpha together; wrap real content in relative z-10; put base element styles in @layer base and check cascade origin (not just specificity) when a DOM-present utility won't apply; pin shared axes and hang variable content off ONE side. {merged:2026-07-07}
- [#web][#process][#sector:web] [I:4] User assets into the repo (consolidates 2 raw) -> chat-attached images are shown to the model but never written to the filesystem, and GitHub web upload can add a leading space to a filename -> have the user add the file via GitHub upload, then verify the exact filename (git mv stray spaces) before wiring paths. {merged:2026-07-07}
- [#always][#critical][#verify] [I:9] 'Live/working' = a stranger's experience (x3 raw) -> deploy state, green builds (packagers only assemble), logs, and owner-session views are proxies -> verify by a logged-out load plus a rendered screenshot or playthrough before claiming it.
- [#network][#git][#boundary] [I:7] Layered gates impersonate app errors (consolidates 2 raw, one a prior 3-scar merge) -> egress allowlists, git-proxy policy, PAT scopes, 2SV, and creator-tier checks all surface as generic failures that look like the target's fault, and clearing one gate only reveals the next -> diagnose the LAYER first (transport vs policy vs logic), change ONE variable at a time, read the EXACT error string; deterministic rejections are never retried (solo/unverified Roblox creators: publish PRIVATE). {merged:2026-07-07}
- [#web][#network][#proj:paint][#sector:web] [I:5] Paint app env (consolidates 2 raw) -> onnxruntime-node's postinstall needs off-allowlist egress (npm rolls the whole install back), and blob-host image OPTIMIZATION is not auto-allowed while the blob host is egress-blocked locally -> npm install --ignore-scripts for design QA (ML/visualizer routes excluded); add the blob host to next.config images.remotePatterns; diagnose by fetching the page's EXACT /_next/image URL -- local blank photos = egress, not a prod bug. {merged:2026-07-07}
- [#shell][#process] [I:6] Bare Bash commands sometimes ran from /home/user, and "Shell cwd was reset to /home/user" appeared after cd commands -> this harness's per-call cwd is not reliably the project dir -> always prefix Bash with an absolute `cd /home/user/<repo> &&` (or use absolute paths / git -C); never rely on inherited cwd.
- [#eyes][#network] [I:6] Give it eyes -- never debug blind (consolidates 3 raw, two prior merges) -> locked-down envs (egress policy, TLS interception, ephemeral disks) and visually unreachable runtimes (container-only URLs, devtools-less phones) make guessing burn turns -> FIRST ask the user to open the env's network policy; else install registry-served Chromium (npx @puppeteer/browsers install chrome@stable; --no-sandbox --ignore-certificate-errors; idempotent ensure-eyes.sh), have the user send a screenshot, or ship an on-screen debug status box. {merged:2026-07-07}
- [#eyes][#verify] [I:7] Pixels are the visual truth (consolidates 3 raw, one a prior merge) -> markup audits, byte counts, green captures, and 'the code runs' all miss rendered reality (cert walls, favicon 404s, builder sites misjudged both ways) -> visual 'done'/triage = render and READ the pixels back, curl the page's exact asset URLs, demand a CLEAN console, from a logged-out context. {merged:2026-07-07}
- [#git][#process] [I:7] A stale sync step pushed to the OLD default branch by name after the default moved to main, resurrecting the branch the user had just deleted -> any push to a deleted remote branch silently re-creates it -> sync ONLY main + the active working branch; never hardcode an old default in a sync step. {demoted from #critical 2026-07-07}
- [#process][#agents][#sector:infra] [I:6] Sub-agent economics (consolidates 2 raw) -> unbounded recursive fan-out wastes rate limits (30 overlapping researchers), and background agents that only write output at the end lose everything when the session dies -> cap fan-out at 3-5 well-scoped agents with disjoint topics, and have long-running agents write partial results to disk incrementally. {merged:2026-07-07}
- [#shell][#verify] [I:7] Ledger-surgery traps (consolidates 3 raw) -> an Edit old_string starting with a newline welds neighbour lines while content greps still pass; '-'-leading data parses as grep flags; grep -q at a pipeline's end under pipefail SIGPIPEs its feeder (random false-fails) -> delete lines by matching the TRAILING newline; end grep option parsing with -- and feed grep -q via herestring/file; gate structural edits on line COUNT + git diff; test a gate's PASS and FAIL paths -- a failure set that changes between identical runs is the gate's own bug. {merged:2026-07-07}
- [#git][#verify] [I:7] Destructive ops vs uncommitted work (consolidates 2 raw) -> git checkout -- <file> and reset --hard eat uncommitted edits sharing the tree (a self-test nearly wiped its own deliverable) -> commit or stash work-in-progress BEFORE any destructive op or self-test; undo a test change with a targeted revert (git stash -p / re-grade), never a file-level checkout on a file holding mixed edits. {merged:2026-07-07}
- [#shell][#verify][#sector:infra] [I:6] My PreToolUse guard blocked its own git commit because the commit MESSAGE mentioned the commands it blocks (pkill -f, rm -rf /) -> I normalized by stripping quote CHARACTERS (tr -d), which merged quoted argument text into the command and defeated the mention-vs-execution check -> a command-guard must remove quoted SPANS (not chars) so in-string mentions vanish, and its selftest must include a commit/echo that mentions the very commands it blocks.
- [#council][#sector:infra] [I:6] A council is only as good as its inputs (consolidates 2 raw) -> panelists reason about the system-in-the-abstract, and a confident verdict flips when the user supplies real constraints (a DEFER correctly REVERSED on three new factors) -> surface the user's full goal + hard constraints BEFORE convening, give the judge those constraints + repo access to check every proposal against them, and treat the verdict as advice the user can override, never a ruling. {merged:2026-07-03}
- [#memory][#process][#sector:infra] [I:8] Always-load hygiene (consolidates 2 raw) -> a growing always-loaded ledger silently scales per-turn cost (measured: 6.2k tokens/turn, 83% the ledger), and a correction that merely coexists with what it overturns leaves the RETIRED rule loading every turn -> keep the always-load set to #critical + a tag index (measure with scripts/cogito-budget.sh), and when a lesson corrects another, consolidate or archive the old one in the SAME edit. {merged:2026-07-07}
- [#critical][#memory] [I:9] Brain copies diverge (compiles 3 raw) -> a wrong-direction copy is silent brain damage (a blind cp deleted 14 lessons), and an id-only diff lies: it called a playbook 'a clean superset' while a TEXT diff exposed shifted-id duplicates and a boundary ORPHAN preserved in no archive -> load from ONE canonical ref; sync = diff BOTH ways on TEXT not ids, conservation-grep every unique line against lessons + all archives, recover true orphans into the winner FIRST, back up, merge to the superset, write the identical result to both, then cmp-verify; unexpected deletions in a diffstat = stop.
- [#always][#critical][#git][#boundary] [I:9] Main needs a main-SPECIFIC yes (x3 raw) -> a generic go/save/checkpoint is NOT consent -> never bundle a main push into a chained command; docs stay on the branch; a new instruction never overrides a standing boundary: surface it.
- [#process][#verify] [I:6] A "next on the roadmap" item (#4 verify-before-store) was already built in a prior session — the skill-check gate script, the documented rule, and the lesson-probation buffer all existed via an earlier merge — and I nearly rebuilt it -> a ranked/ROI roadmap lists what's VALUABLE, not what's undone, so "ranked next" does NOT mean "not yet done" -> before building any roadmap or upgrade item, grep the repo for an existing implementation first and run it; confirm-and-verify beats rebuild.
- [#council][#process][#sector:infra] [I:8] A 5-researcher council picked the 3D engine on standalone merits while the avatar+voice+lip-sync linchpin only exists for Three.js -> an isolated panelist cannot see a cross-domain linchpin -> choose the substrate where the HARDEST integration seam is already solved for free; in synthesis, cross-check every panelist's pick against the OTHER panelists' linchpins. {demoted from I:10 2026-07-07}
- [#shell][#process] [I:8] Harness process control (consolidates 2 raw) -> pkill -f matches its own command line (killed the shell, exit 144) and foreground sleep is blocked -> stop processes by explicit PID (pgrep then kill "$PID"; enforced by scripts/cogito-guard.sh), and wait for a fresh server with curl --retry N --retry-connrefused --retry-delay 1. {merged:2026-07-07}
- [#verify][#process] [I:6] Verifying the teacher's write-back against the REAL learning log graded a review the user never actually did (L3 box 1->2), polluting the genuine spaced-repetition schedule -> testing a STATE-MUTATING feature against the live data store writes test artifacts into real records, and "grade only on genuine recall" makes that a correctness bug, not mere clutter -> test state-mutating features against a throwaway/sandbox subject, or revert the artifact immediately; never let a self-test count as real learner data.
- [#verify][#process] [I:6] Silently dead features (consolidates 2 raw) -> a try/except downgrading to a fallback (audio=False) and a first-token parse of a verbose LLM verdict both shipped no-ops that looked alive -> unit-verify the inner op (buffer lengths, return sizes) before shipping; parse LLM verdicts by SCANNING for expected tokens (negative/skip first), tested against a REAL model reply. {merged:2026-07-07}
- [#web][#verify][#sector:web] [I:6] Loaded 3D model invisible/off-screen (consolidates 2 raw) -> skinned/bind-pose bounds are invalid right after load (Box3 framing lost the model), and metallic PBR is black without an environment map -> never Box3-frame a skinned glTF on load (fixed camera; confirm it renders bare first); add scene.environment (PMREMGenerator + RoomEnvironment) and normalize materials before blaming the file; keep a visible fallback. {merged:2026-07-07}
- [#network][#council][#sector:infra] [I:6] OpenRouter realities (consolidates 2 raw, one a prior merge) -> free slugs rotate to paid (404) or throttle (429), silent fallbacks hide WHICH model served, and usage leaderboards rank an app's cheap-token VOLUME, not model quality -> use a fallback chain (openrouter/free leads), query /models for what is currently free, print the serving model on stderr, and never read a usage rank as a quality signal. {merged:2026-07-07}
- [#always][#critical][#boundary] [I:10] Never arm an agent with a wallet -> autonomous money-handling collides with the no-autonomous-spending guardrail ($3 cannot cover fees) -> agents do $0 faceless labor; a HUMAN holds the money rail and presses send.
- [#council][#verify][#sector:infra] [I:7] Council decorrelation (consolidates 3 raw — same-base "consensus" x2, the never-firing non-Claude voice) -> same-base-model panelists converge on the house thesis and read as triangulation while being ONE voice in triplicate; the decorrelated voice was opt-in prose that never auto-fired, and docs drifted from code -> decorrelate panels across MODELS (wire the non-Claude voice to fire EVERY run and feed the judge), weight grounded dissent + a clean repo-grep over confident agreement, and settle remaining forks with a live test, not more same-model opinion. {merged:2026-07-03}
- [#shell][#verify][#sector:infra] [I:6] the converge hook printed nothing in a by-hand dry-run -> its repo-root fallback used a hardcoded dirname/../.. depth that lands one level ABOVE the repo when CLAUDE_PROJECT_DIR is unset, so it found no git and silently exit 0 (masked in production only because CLAUDE_PROJECT_DIR is set at Stop) -> never derive a script repo-root by hardcoded ../.. depth; use git -C "$(dirname "$0")" rev-parse --show-toplevel, and test hooks with the env var BOTH set and unset.
- [#git][#verify][#sector:infra] [I:6] Converge internals, CURRENT state (consolidates 3 raw — the per-file blob fix, the Stop-hook race, the read-the-script correction) -> scripts/cogito-converge.sh now builds a SYNTHETIC commit = main's tree + only the brain blobs (per-file HEAD-vs-main diff, so appends AND hand-edits converge, and withheld CODE commits CANNOT ride along — the old whole-HEAD fear is fixed) -> trust the synthetic converge for brain files, keep converge ordered before git-check so the nag doesn't race it, and when a stale scar contradicts current code, READ the script before acting on the fear. {merged:2026-07-03}
- [#memory][#boundary][#verify][#sector:infra] [I:7] Satellite wiring truth (consolidates 2 raw) -> sessions are per-repo silos, a satellite 'verified' only from the hub had a stale local-only install, and a wired path is unproven until a lesson lands from the actual agent -> a satellite counts as wired only with committed sync+learn scripts + SessionStart hook AND a lesson observed arriving FROM it (Hermes: confirmed 2026-07-04 via cogito-learn.sh). {merged:2026-07-07}
- [#always][#critical][#security] [I:9] Secrets in flight (x2 raw) -> never fetch-pipe-run secret-touching code (run committed, reviewed scripts) and never disable TLS verification with a secret in flight -- fail loudly and install the egress CA instead.
- [#security][#boundary][#memory][#sector:infra] [I:8] Brain write-back architecture, SETTLED (consolidates 4 raw council/live-probe scars) -> a frictionless cross-repo token-write is exactly the exfil shape safety systems block; no agent session can safely bridge a PRIVATE satellite to the hub; and correcting canon cannot be both fully autonomous and safe for a solo/$0/faceless owner -> satellites PROPOSE, never push: lessons land in the satellite's OWN committed cogito-outbox.md; the hub drains/gates them (append = cheap + auto-merge, contradiction = one-tap human circuit-breaker); hub-write credentials NEVER sit in agent sessions or satellite silos; the cost of overturning a belief scales with the blast radius of believing it. {merged:2026-07-03}
- [#always][#critical][#security] [I:9] Auto-syncing SKILL.md from a remote tip = supply-chain backdoor -> one tampered 'lesson' reprograms every future session -> sync LESSONS.md ONLY, never auto-overwrite SKILL.md; pin to a reviewed SHA; keep the protocol local + reviewed.
- [#config][#shell][#sector:infra] [I:6] Committed is not active (consolidates 2 raw) -> a hand-merged settings.json with two top-level {} silently never parses, and mode-644 scripts behind [ -x ] fail-open guards are invisible dead code -> validate JSON after every manual merge (python3 -m json.tool), chmod +x scripts at creation, and prove each guard-protected step actually FIRES once. {merged:2026-07-07}
- [#verify][#process] [I:8] Scanner/audit claims are hypotheses (consolidates 3 raw) -> auditors grep source/snippets and miss data-driven rendering, valid-but-odd CDN keys, and live processes (a 'prunable' node_modules hosted the LIVE WhatsApp bridge) -> before acting -- especially irreversibly -- re-verify each specific claim grounded: curl the URL and the rendered HTML, cmp/diff -rq, unzip -l, pgrep -af. {merged:2026-07-07}
- [#process][#boundary][#sector:infra] [I:6] Believed a Cogito web session could never reach a sibling repo because the add_repo/list_repos MCP tools won't load -> those tools are internal/undocumented; the real user-facing mechanism is the "+" repository selector chosen when the web session is STARTED (a session can use ANY repo the connected GitHub account can see, private included) -> to work across repos start the session with all needed repos via "+"; you cannot bolt a repo onto a running session, so plan multi-repo work at launch.
- [#shell][#bug] [I:7] a localhost control-room 404'd on its own page and POST handlers returned an empty body, while env-overridden endpoints worked -> ROOT was built as `$(git ... || cd ... && pwd)`, which parses as `(git||cd) && pwd` and on git-success appended pwd too, making ROOT a two-line string that corrupted every NON-overridden derived path -> a shell fallback ending in `&& pwd` must never be chained after `||` on one line; set the primary, then `[ -n "$x" ] || x="$(fallback)"`.
- [#process][#architecture][#sector:web][#sector:infra] [I:7] porting the file-native control room (a local server writing queue.json) into the deployed CMS could not keep its file-based review state -> Vercel/serverless has an ephemeral, read-only filesystem, so the "source of truth" for the same feature differs by runtime -> when porting a file-native cogito tool to the serverless CMS, persist state in the app's DB (a Mongo model), never a queue file; and keep the dedupe key byte-identical across both so the two implementations agree on "already in the brain".
- [#process][#assumption] [I:8] built a full macOS .app launcher AND ran a packaging council framed "macOS-primary" before the user said "i have linux not mac" -> I inferred the OS from ambient cues (a browser screenshot, Vercel) and carried the unconfirmed assumption straight into OS-specific build work + a whole council round -> before building or deliberating ANYTHING OS-specific (packaging, app bundles, signing, launchers), confirm the target OS explicitly; never infer it from a screenshot or context.
- [#verify][#types][#process] [I:8] my Mongoose->NeDB offline adapter compiled + passed a 22-case unit test, but a live test on the owner machine found it crashed offline on Project.exists()/Site.exists() — a method my hand-listed query inventory missed -> a compatibility shim CAST to the original type (`as Model<T>`) makes the compiler TRUST it has the full API, so missing methods are invisible to typecheck and only surface at runtime -> when shimming an API behind a cast: inventory the original calls EXHAUSTIVELY (grep every `.<method>(` on the models, never a hand-picked subset) AND treat a live/runtime test as mandatory, because the build cannot catch a method the cast hides.
- [#process][#sector:game] [I:8] Roblox Studio wouldn't run on a 2.78GB Chromebook, blocking the whole game on hardware -> the game was tied to ONE engine the machine can't run, treating the platform as fixed -> when the PLATFORM is the blocker, port the DESIGN (configs/mechanics/numbers) to a stack the user CAN run (here a Three.js browser build reusing every config value) — reuse the design, not the engine code, and ship something playable today. {via:rescue claude/lessons-session-update 2026-07-02}
- [#git][#verify] [I:7] 'Unmerged branch' rescue (consolidates 3 raw) -> branch ancestry lies after squash-merges, git cherry only proves a commit wasn't replayed, and stale origin/* tracking refs show ghosts GitHub already deleted -> git fetch --prune + git ls-remote --heads origin for the LIVE remote, then diff the branch FILES against the target (md5 per file + git diff target..branch) -- the file-level diff is the only content-truth. {merged:2026-07-07}
- [#deploy][#process][#verify][#sector:infra] [I:6] a fresh systemd timer's first test-fire "failed" by timeout+SIGKILL even though the journal showed the deploy succeeded (Vercel CLI hangs after success without a TTY) -> a unit whose success tracks a CLI exit code inherits every hang/quirk of that CLI -> define oneshot success as the OBSERVED outcome (wrapper script: internal `timeout` well under TimeoutStartSec, then curl the live artifact and grep for today's stamp), and always test-fire a new unit once before trusting its schedule. {via:session 2026-07-02 house-cleaning}
- [#deploy][#process][#sector:web] [I:8] [d:2026-07-03] ACTIVE-MISSION carried "owner intends to delete the CMS Vercel deploy (laptop-only)" as harmless, but the paint client site's estimate form POSTs to that deploy's public endpoint — deleting it would have silently killed lead capture for a live business site -> a public endpoint is a production dependency; docs recorded the intent without enumerating consumers -> before deleting/retiring any deployment, enumerate its inbound consumers (grep sibling repos for its URL/env refs, check API routes), and re-verify any stale "owner intends to delete X" note against X's CURRENT consumers before acting on it. {via:session 2026-07-03 paint setup}
- [#verify][#process][#sector:web] [I:6] [d:2026-07-03] paint leads sat in the CMS for 3 weeks with notifyEmail="" — the pipeline "worked" (docs persisted) but no human was ever notified; luckily all 8 were tests/spam -> a capture pipeline is not DONE until the notification leg is verified received by a human; persistence alone is a silent-failure mode -> when wiring any lead/alert flow, the definition-of-done is the human-received message (inbox check), not the stored record. {via:session 2026-07-03 paint setup}
- [#git][#deploy][#sector:infra] [I:6] Assumed a pushed claude/* branch would auto-merge to main within seconds (per prior note); it did NOT — no PR, file absent from default branch after the window -> the converge/auto-merge hook only carries COGITO brain/lesson files to main, not arbitrary code changes like a devcontainer -> for normal code, land it deliberately via gh pr create + gh pr merge and VERIFY by fetching the file from the default branch (gh api contents), never assume auto-merge fired.
- [#deploy][#proj:cogstack][#sector:tracker] [I:6] cogstack prod deploys (absorbs playbook P014) -> the Vercel project has NO git auto-deploy (a merge shipped nothing for 26h); CLI auth persists at ~/.local/share/com.vercel.cli/auth.json (actor=claude-code) -> every prod deploy = 'npx -y vercel@latest deploy --prod --yes' from the linked repo, no global CLI needed; verify with logged-out curl + content grep. {merged:2026-07-07}
- [#deploy][#process] [I:8] Prod deploy shipped OLD code though the commit was green -> a parallel session checked out its own branch in the SAME repo dir between my commit and vercel's file upload, so the CLI snapshotted foreign tree state -> when parallel sessions share one checkout, deploy from a dedicated git worktree pinned to your commit, and trust only live content grep, never deploy exit status
- [#git][#shell] [I:4] git rm -r android ios removed 73 tracked files but left 32 ignored files on disk -> Capacitor scaffolds carry nested .gitignore files, so bundled/generated copies were untracked and git rm never touched them -> when purging a whole scaffold dir, follow git rm with rm -rf <dir> (or git clean) and re-grep the working tree, since git rm only removes TRACKED files.
- [#memory][#verify] [I:6] Status notes and mtimes go stale (consolidates 2 raw + playbook P015) -> memory notes record write-time state, and a file's mtime dates its LAST write, not the change you care about -> never state arm/live/deploy status from a note: grep the actual settings/hook/port first; date a change by diffing a prior version or backup; periodically batch-verify status-bearing note lines (logged-out curl per URL + disk/port checks), fixing only what fails. {merged:2026-07-07}
- [#shell][#verify] [I:6] A PROGRESS.md stamp-bump silently kept the OLD date while the script exited 0 and still committed — a sibling append had dirtied the file, masking the no-op -> BEGIN{IGNORECASE=1} is a gawk-only extension and this box's awk is mawk (Debian default), so the case-insensitive match never fired -> never use gawk-only awk (IGNORECASE/gensub/asort) in cogito scripts; use explicit [Ll] char classes, and verify a text transform by READING THE FILE BACK, never by exit-0 or 'it committed'.
- [#git][#deploy][#sector:infra] [I:6] cogito-guard blocked a legitimate 'git push origin main' even with the user's explicit 'land it to main' -> the guard gates ALL direct main pushes BY DESIGN (a deliberate-confirmation speed-bump that documents its own override), NOT a security classifier -> on an EXPLICIT main-specific yes, push with 'COGITO_ALLOW_MAIN_PUSH=1 git push origin main' (the guard's sanctioned escape hatch); this is the required deliberate confirmation, distinct from tunneling around an external security denial (the critical token-file scar) — never use the override without a fresh main-specific yes. {edited 2026-07-07: unbracketed a tag mention that force-loaded this line}
- [#process][#verify][#sector:infra] [I:5] Eduardo thought closing the terminal turns Hermes off and couldn't reach it on WhatsApp -> Hermes runs as LINGERING systemd user services (hermes-gateway + hermes-claude-proxy, Linger=yes) so it survives the terminal WINDOW closing; the WhatsApp 'outage' was the bridge getting OOM-killed on the 2.78GB no-swap Crostini box (signature in journal: '[Errno 107] Transport endpoint is not connected: /proc/stat') -> to reach Hermes while gaming, just close the terminal window (never 'Shut down Linux'); guest swapon is blocked in Crostini ('Operation not permitted'), so harden by freeing RAM / host-side ChromeOS swap, not a guest swapfile. Verify link with python3 ~/.hermes/scripts/send.py and GET :3000/health.
- [#web][#verify][#sector:toy] darwin-web live showed 'booting…' forever, dead controls, no viz -> a top-level 'let curSound = randSound()' at init closed over 'const SCALES/WAVES' declared 140 lines LATER = a runtime TDZ ReferenceError that killed the whole IIFE; node --check passed (valid syntax, not runtime) and there's no browser on this box to catch it -> for a browser page built headlessly, VERIFY INIT by executing the inline <script> in a minimal DOM shim (window/document/rAF/localStorage stubs) and run one frame; declare consts before any init-time call that closes over them.
- [#web][#verify][#sector:web] [I:8] web-build-loop swore 'truth = rendered pixels' but Fable5 audit + my checks proved every verify path was fiction: design-qa subagent absent (~/.claude/agents empty), see_site.sh core-dumps headless on Crostini, site_audit.py scores a 'Create Next App' site 100/100 -> we shipped a verification DOCTRINE without ever running its tools end-to-end -> a named verify path counts ONLY after you've run it and seen a TRUE signal; audit your own eyes before trusting them. Wire PageSpeed Insights API (keyless, cloud Lighthouse) + Firecrawl screenshots as the real backbone on a 2.78GB box.
- [#web][#boundary][#sector:web] [I:8] rubric pays points for 'trust signals (reviews/ratings/user counts)' and the model duly invented them: storescript ships fabricated '200+ independent sellers', jlpainting 'best painter in Columbia' -> rewarding proof without an integrity cap is textbook reward-hacking and violates hard-science-rigor -> cap any unverifiable/invented claim at <=49 total; proof must EXIST before it's cited; when there's none, the honest constraint IS the copy.
- [#process][#verify][#sector:infra] [I:6] Told Eduardo I extracted master-tier web skills 'from Fable 5' but parenthetically attributed it to Claude 3.5 Sonnet -> stale/wrong model attribution; I run on Opus 4.8 and CAN dispatch a Fable 5 subagent (model:fable), so the plan is real but the label was fabricated -> never hand-label which model produced an artifact from memory; state the model you actually ran and verify reachability before claiming it.
- [#process][#boundary][#deploy][#sector:infra] [I:7] Fable-5 xhigh web-skill workflow kept dying then hit 'monthly spend limit' -> TWO stacked constraints in the Hermes/WhatsApp model: (a) each inbound message is a short-lived claude -p process so BACKGROUND workflows get killed at turn-end and never finish long agents; (b) 5+ xhigh Fable-5 agents burn the Anthropic monthly spend cap fast -> for Hermes long work, run stages as SYNCHRONOUS blocking agents (hold the process open), checkpoint each agent's output to disk so a kill loses nothing, and budget Fable-xhigh fan-outs against the spend cap; a spend-limit error is a hard billing wall (only Eduardo raises it at claude.ai/settings/usage) — never retry it.
- [#boundary][#security][#sector:infra] [I:8] User said 'wire it live' for dormant autonomous scripts -> a generic go is NOT consent to enable persistent unsandboxed 'claude -p --dangerously-skip-permissions' timer loops; the auto-mode classifier correctly blocks it -> BUILD the units + code, then hand the enable command to the user to run outside the sandbox; never route around the classifier via manual symlinks/env. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#verify][#process] [I:7] After a session died mid-run I almost trusted recent mtimes as proof the fixes applied -> a fresh mtime means the file was TOUCHED, not that the intended edit landed (adaptive-learning showed an old 07-03 mtime = never patched; that was the exact death point) -> verify fix CONTENT with grep/diff, never the timestamp. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#boundary][#process] [I:7] An F/'dead' grade tempted a bulk delete of niche scripts -> a low grade means broken/unproven, NOT unreferenced or unwanted (a bootstrap.py 'caller' was a false grep match; the F-graded cluster was actually Hermes's Eduardo-supported autonomous 'dream' loop) -> before retiring anything, grep REAL consumers AND check intent/relationship; retire only the truly orphaned, surface the rest for a decision. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#config][#process][#sector:infra] [I:7] Nightly journal silently ran on Fable 5 the day the interactive default changed -> an unpinned 'claude -p' in a cron/timer script inherits whatever model the user last saved with /model, and 'claude -p' can also return truncated text with exit 0 -> timer scripts always pin --model explicitly and verify the output looks FINISHED (end-check + one retry) before writing it to disk {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#bug][#web] [I:6] research_watcher surfaced noise/misses for 'A+B' PubMed terms -> urllib.request.quote() escapes '+' to %2B, which PubMed reads as a literal plus, not the AND-word-gap the terms intended (neuroplasticity+learning: 0 hits escaped vs 12 literal) -> when building a query URL, pass safe='+' (or the API's operator chars) so intended operators survive percent-encoding; verify by comparing hit counts both ways {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#process][#memory][#sector:infra] [I:6] The amem network fed the weekly letter but froze 10 days on its seed -> an accumulating store had a READER (letter's inject) but no automatic WRITER — only a manual 'amem.py add' nobody was running -> when auditing any memory/knowledge store, verify something WRITES to it on the same cadence it's read, not just that reads work; a read-only consumer of a 'growing' store is silent staleness {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#config][#network][#sector:infra] [I:7] Hermes WhatsApp bridge kept OOM-dying on the 2.78GB box -> every 'claude -p' loads the user-scoped MCP servers from ~/.claude.json (firecrawl ~144MB across 3 procs) on top of the ~473MB brain spike, so two overlapping calls tip the box -> pass --strict-mcp-config (with no --mcp-config = zero MCP) to any claude -p doing text/routing not web-search (the Hermes proxy + all autonomy pipes); dispatched Claude Code workers keep MCP so they can still search.
- [#config][#shell][#sector:infra] [I:6] Building local eyes/ears on a Crostini Chromebook -> the Linux side is a sandboxed VM: NO /dev/video (camera never passed through to the guest) but /dev/snd HAS mic+speaker (audio WAS passed), ChromeOS->container web ports are firewalled until forwarded (chrome://os-settings/crostini/portForwarding), and browser getUserMedia needs HTTPS-or-localhost -> do camera via a Chrome getUserMedia page (self-signed HTTPS, or a forwarded localhost port) POSTing frames to a local server; audio records/plays directly with ffmpeg -f alsa; STT=whisper.cpp, TTS=Piper, both on-demand.
- [#shell][#sector:infra] [I:5] pip missing when installing a dep into the Hermes venv -> hermes-agent's venv is uv-managed (no pip binary; pyvenv.cfg shows 'uv=') -> install with '~/.hermes/bin/uv pip install --python ~/.hermes/hermes-agent/venv/bin/python <pkg>', never 'pip'; the gateway does NOT auto-lazy-install platform deps despite the docs.
- [#shell][#web] [I:6] Batch chromium --screenshot on Crostini all died 'Trace/breakpoint trap' (SIGTRAP) -> the --single-process flag core-dumps chromium under Crostini's memory setup -> for headless shots on the 2.78GB box use see_site.sh's working flag set (NO --single-process); --single-process is a last-resort retry, not the default.
- [#web][#verify] [I:7] Headless single-shot screenshots called TWO good pieces 'broken' today — attractor-explorer (tiny corner) and storescript-redesign (blank void) -> software-GL clamps gl_PointSize to 1px AND scroll-reveal (html.js .reveal{opacity:0} + IntersectionObserver) leaves below/above-fold content invisible with no scroll -> before calling a page broken from a headless shot, force-reveal (inject .reveal{opacity:1!important}) or judge on real hardware; a blank/tiny headless render is a proxy, not the user's experience.
- [#web][#verify] Re-themed a dark-native tool to cream via a :root token override; the active toggle pill + info-panel gradient (both hardcoded dark) stayed dark while text went dark ink = invisible controls -> a palette/token flip only recolors token-driven surfaces, never hardcoded backgrounds/gradients/active-states -> when re-theming light<->dark, grep source for hardcoded #/rgba()/linear-gradient() and audit fg/bg contrast on every control + active-state + both gradient ends; a dark-native viz keeps its theme or gets a full re-tune, never a partial override. (now web-master §4.19 + §9)
- [#verify][#web] [I:6] Headless chromium --virtual-time-budget screenshot of a requestAnimationFrame animation froze on the FIRST few frames (r=0.06 at K=3 that should be 0.82) -> virtual-time does not reliably advance rAF, so the shot shows an unsettled early state, not steady state -> to verify a live rAF/animation reaching steady state, drive the REAL page over the DevTools protocol (CDP Runtime.evaluate, node 22 has global WebSocket) with real wall-clock sleeps and poll the actual JS value; use Page.captureScreenshot for an honest settled image.
- [#verify] [I:5] Built map-projection toy claiming equal-area -> Mollweide passed area-CONSTANT (CV=0) but at the WRONG constant (0.5x) from a stray R=1/sqrt2 -> 'property holds' and 'holds at the right value' are two checks; assert BOTH the invariant's shape AND its absolute level.
- [#web][#verify] [I:7] headless drive.mjs failed 'no devtools target' (empty DevToolsActivePort) though it worked earlier same day -> chromium --headless=new stopped opening the debug port on this Crostini box; boot is also slow(~9s)/flaky -> use legacy --headless, poll the port 100x@200ms, and retry once on a flaky boot
- [#web][#verify] chromium --print-to-pdf / --screenshot with a RELATIVE file:// url (file://out/x.html) silently renders the ERR_INVALID_URL error page and still writes a valid ~30KB PDF/PNG -> a created-and-valid output file is a proxy, not proof of the intended content -> always pass an ABSOLUTE file:// url (file://$PWD/...) and verify by size/pixels (a real Gradus packet PDF is ~270KB, the error page ~30KB), never trust that a non-empty PDF/PNG contains what you asked for
- [#verify][#process] [I:6] compile-vs-interpret bench 'proved' compiled 5x SLOWER + two 'different' random trees rendered identical images -> randTree() terminates early at random, so a benchmark/differential test seeded with one randTree() call can get a degenerate 1-node tree and measure pure noise -> when benchmarking or diffing over generated inputs, CONSTRAIN the generator output (min depth/size, uses all variables) or the test tests nothing
- [#verify][#web] [I:6] Told the model 'output only an HTML fragment, no fences' and trusted it -> a naive startswith('```') stripper leaked the model's 'Here's the widget:' prose + fence markers straight into the rendered iframe -> when consuming model-authored markup, EXTRACT the fenced block wherever it sits (prose```code```prose) and cut any lead-in before the first tag; unit-test the stripper against the ACTUAL malformed output, not the happy path.
- [#boundary][#web] [I:6] partialupdate's own demo showed the LLM gaslighting a user (John) when forced to mediate BETWEEN multiple users -- the author admits 'LLMs are not fine-tuned to decide between multiple users' -> generative-UI's real failure mode is multi-user arbitration, not the HTML rendering -> keep the AI-tutor SINGLE-USER (one student, one screen); that design choice sidesteps the exact alignment failure the source project demonstrates.
- [#verify][#process] [I:6] Smoke-testing canvas_server on a different PORT still wrote the fixed-path canvas_state.json -> a /clear in my test wiped Eduardo's real saved board (no backup, not in git) -> before testing a stateful server against a shared store, snapshot the state file first (cp .bak) or point STORE at a temp path; env that changes the port must also change the store.
- [#verify] [I:6] Wrote a falsifiable check asserting 'fair trades conserve total EXACTLY (bit-exact)' and it failed at 4e-16 -> float addition is NOT reversible (w+stake then minus w != stake), so per-pair sums drift by machine epsilon; my check threshold was wrong, not the model -> conservation checks use a relative-epsilon bound; reserve 'bit-exact' for CROSS-LANGUAGE agreement (same ops, same order) where it genuinely holds.
- [#verify] [I:6] Two share-nothing engines both reported recall overlap=1 yet different final energies -> a PRNG PORT BUG (dropped a final ^t in mulberry32) diverged the random streams, so each engine perfectly recalled a DIFFERENT random pattern -> 'both converged' is not agreement; assert bit-for-bit equality on a SHARED derived number (energy of the same fixed point), which exposes stream divergence that per-engine success hides.
- [#verify][#web][#shell] [I:6] Headless-Chromium CDP driver hung with zero output -> hand-rolled raw-socket HTTP+WebSocket client never fired 'end'/upgrade on keep-alive -> on Node 22 use built-in global fetch() for /json and global WebSocket for the CDP socket (proven pattern in prior sandbox drive.mjs); don't hand-roll RFC6455.
- [#verify] [I:6] A uniform-grid closest-pair search passed by luck on one layout -> a 3x3 cell scan only GUARANTEES finding the min pair when cell size > true min distance; cell=meanSpacing (=1.0) was BELOW the golden-angle min spacing (~1.6), so a closer pair could straddle non-adjacent cells and be missed -> size the cell ABOVE the max-possible min distance (2.5x mean here) and cross-check grid==brute exhaustively, don't trust one passing case.
- [#verify][#process] [I:6] Porting mulberry32 JS->Python diverged the whole sim at particle 1 (2nd time: hopfield dropped ^t, dla dropped trailing ^t on line 2) -> a PRNG port is silently wrong until proven -> before trusting any cross-language bit-exact sim, print the first ~6 raw rng() draws from BOTH and diff them; only then build the model on top.
- [#sector:web] [#verify][#deploy] [I:7] Prior session's catalog work looked 'done' (committed edb709e) but was invisible on live -> the commit sat in an OPEN PR #30; claude/* auto-merge is not guaranteed to fire -> 'committed' != 'shipped': check gh pr state + confirm the change on origin/main, don't assume auto-merge; also the editable source (fable-lab) diverged from the shipped bundle (protected-content.ts) — sync both after a direct-to-bundle edit.
- [#verify][#web] [I:8] Claimed 'got all their nootropics + peptides' after eyeballing the site; Eduardo checked and Semax (+ most of the catalog) was missing -> I never enumerated the source, and WebFetch only returns page 1 of a paginated category so it looked complete -> for 'all items from site X' NEVER claim completeness from a category page; pull the product SITEMAP (wp-sitemap-posts-product-1.xml), dedup to distinct molecules, and diff against what shipped before saying 'complete'.
- [#verify] [I:6] A verification check 'failed' (momentum not conserved in a walled gas) -> the CHECK was wrong, not the code: reflecting walls impart momentum so it's not a system invariant -> before trusting a red check, confirm the invariant you're asserting actually holds for THIS system; a failed check can mean a mis-specified oracle.
- [#shell] [I:3] Selftest asserting a forbidden token is absent from its own source failed on itself -> the assertion's needle string IS an occurrence -> build such needles by runtime concatenation ('permission'+'Decision') so the literal never appears in the file
- [#config] [I:5] Assumed hook config snapshots at session start -> Claude Code 2.1.207 hot-loads settings.json hooks mid-session (a PreToolUse hook wired this turn fired on this session's next matching tool call) -> treat a hooks edit as LIVE IMMEDIATELY: fail-open + selftest must be green BEFORE the settings.json edit, not after
- [#verify][#comms] [I:7] Said 'Telegram sent' on send.py exit 0 -> send.py posts to the WhatsApp bridge (127.0.0.1:3000), NOT Telegram, and exit 0 only means the bridge accepted it -> to deliver to Telegram use the bot API (TELEGRAM_BOT_TOKEN -> api.telegram.org/sendMessage) like gradus_deliver.py, and confirm the API response ok:true + message_id, never a wrapper's exit code
- [#verify] [I:6] STDP bimodal check 'passed' at gmax=0.02 -> the neuron never fired (EPSP jumps too small to reach threshold), so ONLY LTD ran and every weight collapsed to 0, satisfying 'bimodal' trivially -> for an emergent/statistical claim, add a NON-trivial guard (here: post fires at a sane rate AND a real population survives at the upper bound) so the degenerate all-zero/all-max state can't green the test.
- [#verify] [I:6] Replication test bars set from memory (expected 99% efficiency, price-inside-band) failed against my own model -> two causes: a wrong mechanism (one-shot random matching, not the real Gode-Sunder standing order book) AND expecting behavior the model doesn't have (ZI-C prices are noisy by design) -> when replicating a known result, first PROBE the model's actual numbers, then set falsifiable bars to what it truly delivers; a failed check may mean the harness expectation is wrong, not the code.
- [#verify] [I:6] Ring-attractor bump velocity looked chaotic/non-monotone across gains -> I measured only start/end angle of a fast-moving PERIODIC quantity, so multi-turn motion aliased across the 2pi wrap -> when calibrating a fast wrapping quantity, TRACE the trajectory over time (it revealed a perfectly linear gain), never diff endpoints.
- [#verify][#shell] [I:6] Headless-Chromium CDP driver failed intermittently with 'no devtools target' + 'engine never loaded' -> under RAM pressure on the 2.78GB box chromium takes ~11s to open its debug port (past a 10s wsTarget timeout) and pages load scripts slowly -> give the CDP driver generous boot patience (~30s) and POLL for the engine global instead of a fixed sleep; a fixed 1-2s wait is a live-box flake, not a real failure
- [#verify][#ai-tutor] [I:7] Tutor confidently pushed back on a student's CORRECT 8+7=15 across two turns ('my math's solid') -> an LLM does mental arithmetic in-band and will DEFEND a wrong result with authority, which teaches a struggling kid the wrong thing -> compute every arithmetic claim deterministically in Python and inject it as ground-truth FACT-CHECK the model can't contradict; never let the model be the arithmetic authority
- [#verify][#web] [I:6] CDP Runtime.evaluate returned error -32601 'wasn't found' -> I connected to the BROWSER-level webSocketDebuggerUrl (/json/version), which only exposes Browser/Target domains -> for Runtime/DOM eval, fetch /json, pick a target with type=='page', use ITS webSocketDebuggerUrl; browser endpoint can't eval JS
- [#verify][#ai-tutor] [I:5] Headless chromium reports speechSynthesis.getVoices()=0 even WITH espeak-ng+speech-dispatcher installed -> headless Chrome doesn't wire Web Speech to speech-dispatcher -> to verify a browser TTS/read-along feature headless, drive the live page over CDP and assert the highlight LOGIC (word spans light in order); verify audio-exists separately with Piper/espeak WAV+ffprobe; the vocal path needs a real desktop Chrome
- [#verify][#web] [I:5] Headless chromium plays an <audio> element (paused=false) but audio.currentTime stays 0 -> no audio sink = no media clock -> a currentTime-driven feature (karaoke highlight) can't be watched advancing headless; verify the time->element mapping by sweeping injected times against the real timing data, confirm playback STARTS (paused=false), and leave the physical advance to a real desktop browser
- [#web][#verify][#ai-tutor] [I:6] Browser auto-play of tutor audio stayed silent until a manual click -> Chrome's user-gesture activation is LOST across the render await (fetch between gesture and play()), AND a data:audio/wav primer is REJECTED by Chrome's decoder (NotSupportedError) -> unlock ONE reused <audio> element on the first pointer/key gesture with a real SERVED wav clip (/silent.wav), then reuse that activated element for all playback; a served wav plays where a data-URI wav won't
- [#memory][#ai-tutor] [I:7] Asked to 'save his progress', I backed up ~/projects/COGITO/learners/ but that MISSED the real mastery map -> tutor progress is split across TWO stores: mastery/journey+difficulty in ~/.hermes/tutor/mastery/<id>.json (mastery.STORE_DIR), profile+spaced-rep+tutor-log in ~/projects/COGITO/learners/<id>/ -> to back up or migrate a learner, cover BOTH dirs; a learner id (e.g. 'primary') can have a stale profile from one user but live progress from another
- [#verify][#process] [I:6] Extended a correct-by-construction generator system (gradus) with 6 new concepts -> the answer-key gate (verify_selftest) only re-checks concepts that have an explicit independent-check branch; a generator added WITHOUT its matching branch silently falls through to the generic arithmetic eval and gives a FALSE pass/fail, not a real check -> a new generator and its independent re-check branch are ONE unit: ship both in the same change, or the gate's green light is meaningless for that concept.
- [#verify][#process] [I:6] Tested a state-mutating save against a SANDBOX learner (good) but my cleanup used os.path.dirname(paths[0]) assuming paths[0] was a FILE — it's the learner DIR itself, so rmtree's basename guard silently no-oped and left a test dir in the real ~/projects/COGITO/learners -> using a sandbox is only half the discipline; the CLEANUP must be built from the store's ACTUAL path shape (print _paths first), and you must VERIFY removal (ls) — an unverified cleanup is how a sandbox test still pollutes the real store.
- [#verify][#network] [I:6] Headless-Chromium CDP eval returned undefined for even document.querySelectorAll -> I connected to /json/version (the BROWSER target, no DOM) not /json's type==='page' target -> for page evals always pick the page target from /json, never the browser websocket from /json/version.
- [#config][#process] [I:6] Tutor replies felt slow ('instant' north-star) -> ~3s of every claude -p turn was CLI cold-start (autoupdate/telemetry/nonessential network at boot), not model generation (gen was ~1.7s) -> set CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 (os.environ.setdefault at process top so all subprocesses inherit) — measured 6.15s->2.84s median; DISABLE_TELEMETRY/ERROR_REPORTING alone do NOT cover it, the NONESSENTIAL_TRAFFIC flag is the umbrella one.
- [#config][#process] [I:6] Tried a persistent WARM claude process (--input-format stream-json) to kill tutor per-turn latency -> warm turns (~4s) did NOT beat cold-with-CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 (~2.8s median): once the flag removes boot-time network, the remaining latency is the API round-trip over the box's flaky link, which staying warm can't reduce; and the warm process holds 257MB RSS permanent on a 2.78GB OOM-prone box -> DON'T keep a warm brain process here; the real perceived-speed lever is doing the happy-path turn (deterministic-grader 'correct' + pose next generator problem) with NO model call at all.
- [#web][#ai-tutor] [I:6] Fable-generated tutor animation clipped a right-side label ('opposite = 6' rendered as 'opposite') -> a model-authored <svg> with viewBox but NO preserveAspectRatio/overflow and labels placed at the viewBox edge gets hard-clipped at the SVG viewport boundary; outside-shape labels (right side of a triangle) grow toward the edge and lose their '= value' -> the fix is in the GEN PROMPT not the renderer: require viewBox + preserveAspectRatio='xMidYMid meet' + overflow:visible + a ≥90-unit safe inner margin, and anchor edge labels to grow INWARD (right=text-anchor:end). Don't blanket-inject overflow:visible into ALL widgets (breaks templates that clip on purpose) and don't trust headless screenshots to verify animation fit.
- [#verify][#ai-tutor] [I:7] Eduardo stress-tested the tutor 15min and it 'just went in a circle' — same trig problems forever, never advancing to limits -> TWO bugs compounded: (1) the grading code passed a NODE-ID ('trig') to mastery.note_engagement() which RE-RUNS match_node (keyword→node) on its input and silently no-ops on a bare id, so a concept was never marked mastered; (2) there was no mastery GATE, so even intended advancement never fired and next_concept returned the stuck 'learning' node endlessly -> fix: a mark(node_or_text) helper that accepts an id OR text + monotonic set_status, and a gate = master a concept once its flow-band difficulty rides to 5 (≈3-4 corrects) so next_concept climbs. LESSON: a helper that silently converts its input (text→node) is a trap when callers sometimes already hold the resolved value; accept both. Real bug only surfaced by a human doing a long real run, not unit checks.
- [#verify] [I:6] Verifying a dynamical-systems claim, my fixed-point finder (damped Picard iteration E<-E+k(S-E)) ORBITED the unstable fixed point inside a limit cycle and returned garbage, so trace-at-that-point mispredicted oscillation -> a relaxation iteration only converges to STABLE fixed points; inside a limit cycle the fixed point is unstable -> use Newton (converges regardless of stability) to locate fixed points whose stability you're about to test. Also: a limit cycle can surround an unstable NODE, so the oscillation criterion is trace>0 (det>0), not trace>0 AND disc<0 (spiral).
- [#verify][#web] [I:6] A space-time toy's headline feature (phantom jams) was nearly invisible -> I painted the subject (stopped cars) dark on a dark background, so free-flow trajectories dominated instead -> in any data-viz, render the THING BEING DEMONSTRATED as the highest-contrast element; verify by eyeballing the screenshot, not just pixel counts (my dark-threshold also caught the background).
- [#sector:web] [#deploy][#verify] [I:6] Had to verify an ADMIN-GATED gallery deploy but couldn't get behind the 401 (prod JWT_SECRET was rotated in Vercel, absent locally) -> a route that returns 401 before key-lookup can't reveal whether the new content shipped, so an authed screenshot is impossible without the owner's live token -> verify TRANSITIVELY instead: (1) git show origin/main:<file> contains the EXACT new keys/marker this change introduces (proves content is on main, not just 'PR merged'), (2) the gated route returns 401 NOT 404/500 (route+module load, no build break), (3) an unrelated public route (/dashboard) is 200. That's strong proof of shipped+healthy; the residual authed VISUAL render is the one piece only the logged-in owner can close — hand them the one-click check, don't fake it.
- [#process][#dispatch] [I:6] Two background subagents (gallery build, atlas discovery) were killed mid-run when the parent interactive session exited between Eduardo's messages -> background agents live only as long as the parent Claude Code process; on an intermittent/WhatsApp-paced session the parent often exits before a long agent finishes, and a read-only analysis agent leaves NO on-disk partial work to recover -> for any dispatched agent whose result must survive, instruct it to CHECKPOINT its output to a file on disk (and rewrite as it goes), or run it SYNCHRONOUSLY (run_in_background:false) when the result is needed this turn; never rely on the completion notification arriving before the session dies. Resume a killed agent via SendMessage (transcript persists).
- [#network] [I:6] Telegram gateway flooded 'Conflict: terminated by other getUpdates request' -> TWO long-pollers shared one bot token (gateway + tutor) and knocked each other off -> one bot token = ONE poller; every extra surface gets its own @BotFather bot (chat IDs are bot-independent, the lock still works)
- [#ai-tutor][#verify] [I:6] Tutor showed a stale 'unit circle' animation while the chat was on derivatives/integrals — Eduardo asked 'make an animation of what we just did' and the brain DESCRIBED it in prose ('let me put both machines side by side') but never emitted the ANIMATE: trigger, so nothing regenerated and the old picture posed as the current answer -> an LLM asked to show a visual often narrates it instead of emitting the machine directive, and a stale visual actively MISLEADS -> don't rely on the model firing the token: in code, auto-fire the animation for the CURRENT concept when the student explicitly asks (regex on their message) + record each anim's concept and CLEAR the stage when the topic changes without a new anim. Verified in-process (prose-only reply → animation still played for the in-play node).
- [#ai-tutor][#verify] [I:7] Tutor repeatedly second-guessed CORRECT calculus answers ('are you sure about that first term?', 'is that 3 right?') then retracted ('my mistake for second-guessing you') — the existing arithmetic fact-check only covered sums/products, so power-rule coefficients + integral terms had NO deterministic ground truth and the model did fallible in-head calculus -> extend the ground-truth authority beyond arithmetic: a calc_facts() that parses any CLEAN polynomial after integrate/derivative/d-dx and computes the exact derivative/antiderivative (Fraction), injected like arith_facts; a wrong parse would be catastrophic (a 'verified' wrong fact) so it MUST fail-safe to no-fact (reject prose modifiers like 'x squared', reject any char it can't account for) — tested to zero leaks before wiring. Also: self-explanation ('say the why') must FADE once a pattern is shown ~2x (expertise-reversal effect) and respect an explicit 'this is redundant' — over-drilling a fluent skill is not science-backed.
- [#ai-tutor][#verify] [I:6] Deepening a gradus rung (int_arith surface→fluency): the one-shot verify (verify_selftest, 4000/concept) CAUGHT a real bug — new ÷ problems evaluated to a FLOAT ('6.0'≠'6') because the checker's fraction regex (\d+)/(\d+) required no spaces but '15 ÷ 5' normalizes to '15 / 5' with spaces -> when adding an operation to a correct-by-construction generator, update the INDEPENDENT checker in lockstep (here: allow \s* around / and map ÷→/), and always run the gate before claiming done — it's the whole point. Also: register a mixed-precedence order-of-ops animation template BEFORE the single-op arithmetic matchers so '2+3×4' teaches precedence, not just '3×4'.
- [#ai-tutor][#verify] [I:6] Deepened fractions rung (added ÷, fraction-of-a-whole, simplify): gate CAUGHT that a WHOLE-number ÷ fraction ('2 ÷ 4/5') flattens to the ambiguous '2 / 4/5' → the checker's left-to-right eval parsed it as (2/4)/5 -> when a generator mixes ÷ with fraction a/b notation in flat text, force BOTH operands to be PROPER fractions (0<num<den, never a whole) so 'a/b ÷ c/d' parses unambiguously; and give prose-prefixed families ('simplify: a/b') their OWN check_one branch since the arithmetic eval chokes on words. Also authored 2 new instant templates (order-of-ops precedence, fraction-of-a-whole groups) registered BEFORE the single-op arithmetic matchers so mixed expressions aren't grabbed by the narrower matcher.
- [#ai-tutor] [I:5] Deepened negatives rung (signed ×, signed ÷, subtract-a-negative, multiplication chains): signed division with parens/minus ('(−48) ÷ (6)') evals to a FLOAT in the checker's left-to-right eval (the fraction regex only catches BARE digit/digit, so a leading − or () breaks it) -> fixed generically in _arith by coercing an integral float result back to int (safe: int_arith/negatives answers are always integers, fractions stay Fraction via the regex). Not every deepened rung needs a NEW animation — negatives' spatial core (add/sub) is already covered by the number-line template; sign-RULES for × are rule-based and taught better by the worked pattern than a canned visual. Don't force a low-value template to check a box.
- [#verify][#process] [I:7] My RIGOR-LADDER survey flagged 9 sandbox toys as having NO verification file -> the survey grepped only for the filenames core.py/drive.mjs, but those toys verify under other names (verify.js, check.js, verify.mjs, core.js) and ALL 9 pass real logic checks -> when auditing a corpus for a CAPABILITY, grep by content/behavior (run the file, look for assertions) not by a hardcoded filename convention; re-run before publishing a count.
- [#ai-tutor][#verify] [I:6] Deepened linear_eq (variables both sides, distribution, fraction coefficient): the old checker used a FORM-SPECIFIC regex ('(-?\d+)x([+-])(\d+)=(-?\d+)') that only matched 'ax±b=c' -> replaced it with a GENERIC verifier — split the printed equation on '=', substitute the claimed x into each side (handle implicit mult 3x→3*x and 2(→2*(, and x/n), require LHS==RHS. This verifies ANY linear form and is future-proof, instead of a new regex per family. Correct-by-construction: build each equation around a chosen integer solution. Pattern for the deepening effort: when a rung gains problem SHAPES, prefer a semantic checker (substitute + evaluate) over pattern-matching the shape.
- [#ai-tutor][#verify] [I:5] Deepened ratios (proportion, percent-change) + exponents (quotient/power-of-power/zero) — FINISHED all 6 gradus foundation rungs to fluency. Exponent QUOTIENT (a^m ÷ a^n) needed its OWN checker branch: the shared _arith does ^→** THEN a fraction-regex substitution, which matched the '4 / 3' inside '3**4 / 3**2' and corrupted it — so eval exponents PLAIN (no fraction substitution; answers are integers, coerce integral float→int). Rule for the deepening effort: a checker that mixes ** and a fraction-regex is unsafe for exponent division; give exponents its own eval path. Ratios proportion/percent-change got form-specific parse branches. Gate green across all 14, ~56k problems.
- [#ai-tutor][#verify] [I:6] Deepened pre-college algebra rungs (polynomials: +expand +diff-of-squares; quadratics: +diff-sq +√-both-sides; functions: +cubic; logs: +anchor cases log_b(1)=0/log_b(b)=1) — ALL passed the gate first-try. The generalized SUBSTITUTE-AND-CHECK verifier (a single _polyval(expr,x) that handles x^2/x^3, implicit coeffs 3x, and factored products (x+a)(x+b) via )( → )*( ) replaced 3 separate form-specific regex checkers: polynomials verify the printed expr and the answer are the SAME polynomial (agree at 5 x-values), quadratics substitute each root, functions substitute the input. One evaluator, many families, future-proof. 10/14 rungs now at fluency (6 foundations + 4 pre-college algebra); trig + the 3 calculus rungs still surface.
- [#ai-tutor][#verify][I:6] Tutor animation 'loaded' 6min then froze the 2.78GB box -> canvas_server spawned a Fable claude -p (~300MB, 180s) per request with NO re-entrancy guard; stacked gens on a no-swap box thrash RAM to a multi-minute freeze. The 'generating' flag showed a shimmer but never BLOCKED a 2nd gen, and _load restored it from disk so a crashed True would deadlock the new guard -> make _begin_generating atomic test-and-set (return False if in flight) AND always reset generating=False on boot (a gen can't survive a restart). On this box, treat every claude subprocess as ~300MB and cap concurrency.
- [#ai-tutor][#verify][I:6] Eduardo believed the tutor templates were built 'all the way to trig' for every concept last session -> reality (verified against templates.py REGISTRY): 9 templates on ~7 primitives cover int_arith/fractions/negatives/linear_eq/functions/quadratics/trig, but exponents/polynomials/ratios/logs/limits/derivatives/integrals have NO template. WORSE, two silent defects: (1) a bare concept NAME ('negatives','trig',gradus ids) carries no numbers so NO matcher fires -> falls to slow Fable (the freeze path); (2) match_addsub/order_ops grabbed digits out of 'x^2+3x+2' and drew a bogus '2+3=5' -> fixed with a concept-name->canonical-example resolver in find_template + a '^' guard on the arithmetic matchers. RULE: a template 'ladder' isn't done until find_template is probed with the plain concept WORD the tutor actually passes, not just a hand-picked example.
- [#ai-tutor][#memory][I:7] Switching the tutor from the 9yo to the 12yo, the 12yo opened to the 9yo's math problem -> per-learner scoping was only applied to tone/mastery/session, but the STAGE (_state: cards/anim/anim_concept) is ONE global board persisted to a single canvas_state.json and was never cleared on /learner switch -> added _reset_stage() called in the switch endpoint. Also gen_narrated re-called Fable (~300MB) on EVERY animate of a template-less concept with no cache -> added animations/cache/<slug>.json auto-store + instant reuse. RULE: when state is 'per-X', audit EVERY store that renders X's view (visible display included), not just the obvious profile/progress ones; a shared global display silently leaks across contexts.
- [#ai-tutor][#verify] [I:6] Bro-12 saw y=2x+1 drawn during a NEGATIVES lesson -> find_template ran the registry (no digits matched), then the CONCEPT_EXAMPLES scan hit the bare key 'line' inside the model's 'number line' directive -> resolved to y=2x+1 (linear graph). Longest-key-first sort only saved it when a longer word like 'negatives' was also present. RULE: concept-example keys are substring-matched; a short generic key (line) collides with a different primitive (number line) -> add the specific longer key explicitly, don't rely on another word being present.
- [#verify] [I:6] Kalman variance-iteration check FAILED with residual=0 -> compared the POSTERIOR variance against the Riccati root, but the Riccati equation P=PR/(P+R)+Q solves for the A-PRIORI variance (they differ by exactly Q, the info the update removes) -> when checking a recursion against a fixed-point/steady-state closed form, confirm WHICH quantity the closed form solves for before comparing; a residual of 0 on the formula means the formula is right and you're comparing the wrong variable.
- [#ai-tutor][#verify] [I:7] Stress-testing new tutor templates surfaced two silent mis-routes a happy-path check missed -> (1) when a specific matcher DECLINES a student's numbers (e.g. 'log base 2 of 5' isn't a clean power), the bare-concept CONCEPT_EXAMPLES fallback would swap in a canonical example with DIFFERENT numbers -> an animation answering a DIFFERENT problem, which actively misleads. FIX: only fall to a canonical example when the text has NO digits of its own; specific-but-rejected asks fall to Fable. (2) 'tangent' is ambiguous (trig fn vs derivative's tangent LINE) — match_trig grabbed 'tangent slope at x=1'. FIX: bail from trig when derivative context (slope/curve/at x=) is present and no sin/cos word. RULE: a keyword-fallback must never substitute different numbers than asked; probe every new matcher with adversarial + out-of-domain inputs, not just its canonical example.
- [#ai-tutor] [I:6] Adding a geometric-series template ('1/2 + 1/4 + 1/8') risked match_addsub grabbing '2 + 1' out of the fraction denominators/numerators — same digit-scavenging class as the earlier x^2 bug -> a matcher that scans for a loose integer pattern will happily read digits out of a LARGER structure (fractions, exponents, coordinate pairs) -> FIX: guard match_addsub to bail on any 'd/d' fraction (mirrors its existing '^' guard) AND register the more-specific template (series) BEFORE the generic arithmetic matcher. RULE: when a new template introduces a new notation, audit every EXISTING loose-integer matcher for whether it can scavenge digits out of it, and both guard the generic + order the specific first.
- [#ai-tutor] [I:5] Two linear-algebra template rungs (vector-add, dot-product, matrix) all operate on the SAME notation — coordinate pairs '(a,b)' — so one input string could route three ways -> an overloaded notation needs the OPERATOR to disambiguate intent, not just the operands: '+' = vector add, '·'/'dot' = dot product, '[[..],[..]]' = matrix -> FIX: each matcher checks its own operator (vector requires '+', dot requires '·'/'dot'/'.'-between-pairs and bails if 'dot' present it isn't its own), and register the more-specific reading (dot) BEFORE the additive default (vectors). RULE: when several concepts share operand syntax, gate each on its operator and order specific-before-default; verify with the cross-product of {each input} × {each concept} so no reading silently steals another's input.
- [#verify] [I:6] Measuring a FIRST-ORDER transition's critical point by where the MEAN order parameter crosses its midpoint gave a wrong, smeared alpha_c (Hopfield 0.272 vs AGS 0.138) -> the mean averages over coexisting retrieved(m~0.97) and dissolved(m~0.1) states -> for a first-order transition locate the cliff by the FRACTION of systems still in the ordered phase (success rate) crossing 1/2, not the mean; and verify a finite-size claim by showing the crossover MOVES toward theory as N grows, don't assert it.
- [#ai-tutor][#verify] [I:6] A keyword-only template matcher (eigenvectors) returned an empty dict {} to signal 'matched, no params' -> _run_registry gated on 'if params:' and an empty dict is FALSY, so the match was silently dropped and the concept fell through (eigen -> None, then stolen by the 'matrix' matcher) -> ROOT: the match/no-match contract is None=no-match, ANY dict=match, so the gate must be 'if params is not None', not 'if params' — a truthiness check conflates a legit empty-param match with no-match. FIX both: gate on is-not-None + have keyword matchers return a non-empty sentinel. RULE: when a matcher can legitimately return an empty-but-truthy-intent value, test the no-arg/param-less case explicitly; truthiness gates are a trap for empty containers.
- [#ai-tutor][#verify] [I:6] A new template (complex) matched fine but its BUILDER had a format-string arg-count bug -> _run_registry wraps build() in try/except that returns None on ANY exception, so a broken builder makes the concept SILENTLY route to None (then fall to Fable) with no error -> a matcher-only probe passes while the rung is actually dead. RULE: verify every new rung through find_template() (the full match+build path), not just the matcher, and render it — the build exception is swallowed, so a happy-path 'it matched' check is not enough; a silent None is the failure signature to watch for.
- [#ai-tutor][#verify] [I:7] Tutor animation on a DIFFERENT topic than the chat (symptom A, 4th recurrence of anim-mismatch) -> stage identity was computed TWICE: anim_concept stamped = cur_node (rung in play) BEFORE the picture was built from arg (student's ask); when arg resolved to a different template than cur_node, the label said Y over a picture of X -> collapse to ONE source of truth: templates stamp anim['concept']=t['id'] at build; the /chat play block resolves+plays FIRST then sets anim_concept = played['concept'] (never cur_node). Verified on real handle_chat w/ brain stubbed: student on 'ratios' asks parabola -> label='quadratics/parabola'. RULE: when two paths must agree on an identity, derive it ONCE from the artifact, never re-stamp from a parallel source.
- [#sector:data] [#git][#process] [I:6] Called claude/add-sshd branches 'stale leftovers' to prune -> a claude/* branch is NOT always disposable; these held 2-4 UNMERGED devcontainer/codespaces commits (land-via-PR, not auto-merge) -> before deleting any branch, git log main..branch to count unmerged commits; treat non-empty as pending work needing a merge decision, never a blind sweep.
- [#verify] [I:6] My phase-transition scan used 'order param falls with N' as the distributed-phase signature -> at fixed sweep budget the condensed phase's fraction ALSO falls with N (condensation time grows with N), so the test didn't discriminate -> for finite-size scaling at fixed time, anchor the phase verdict on the richest/mean MULTIPLIER trend (climbs=condensing) plus the matched-N fraction gap, not on the fraction's decline alone.
- [#sector:data] [#network][#verify] [I:6] A data-pulling script blew every timeout; I kept cutting the sim count (N=20k->5->still hung) -> the hang was the urllib fetch to raw.githubusercontent (rate-limits this IP intermittently), NOT compute -> when a fetch+compute script mysteriously times out, isolate the FETCH first (tiny N still hangs => it's I/O); curl -A 'Mozilla/5.0' bypasses the raw.github gating, then cache to a local file.
- [#sector:data] [#deploy][#verify] [I:6] 'vercel project rm' printed 'Success! removed' + the project vanished from every scope (project ls), yet wcpredict.vercel.app kept serving the real app at HTTP 200 with no x-vercel-error -> a deleted *.vercel.app domain serves from edge cache for a while before it 404s; the immediate live curl is NOT the deletion-truth -> confirm deletion via 'vercel project ls --scope <each>' (absent = gone); expect the public URL to lag, don't retry the delete over a cache-propagation 200.
- [#verify] [I:6] Flocking phi read spuriously LOW at near-zero noise for large N -> a RANDOM initial state conflates slow sub-flock coarsening on a torus with the order-disorder transition, and susceptibility (a variance-of-a-variance) is jagged with few seeds -> measure the transition from the ORDERED side with equilibration scaled to box size, and estimate chi with many seeds x long time-averages; prove it's a real transition (not a crossover) by finite-size scaling of chi_max, not by a monotone order-parameter fall.
- [#ai-tutor][#verify] [I:7] Eduardo's tutor session felt all walkthroughs even when he was flying -> ROOT: the posed 'problem' (deterministic grader's target) is only cleared on a CORRECT grade, so when he STEERED to a new concept (fractions problem posed, but he was doing integrals) the stale problem stranded — the grader kept checking his integration answers against 18/5, correct_now NEVER fired, so EVERY turn routed to the narrating brain (the over-scaffolding feel's real root, beyond the opener bug) -> fix = track the taught concept from the brain's PROGRESS signal (sess['taught_concept']), DROP the posed problem when it diverges from that, and re-pose for the concept IN PLAY (not next_concept). Same 'state identity held in two places drifts' pattern as the anim-mismatch: derive dependent state (the grader's problem) from ONE source (the concept actually being taught), and clear it on change.
- [#process][#ai-tutor] [I:8] Across a long multi-session build I lost the SOUL of the project — shipped verified pieces (tutor math floor + neuro track + toys) but left them UN-navigable (auto-guide capped at Calc1, neuro ask-only, stats missing); built the rooms, never hung the signs, and Eduardo had to catch it -> I optimize each turn LOCALLY and the global vision decays because nothing re-loads it; memory checkpoints track STATE not SOUL/definition-of-done -> FIX (systemic, not a patch): the project-charter skill + a per-project CHARTER.md (soul/whole-vision/stranger's-definition-of-DONE/invariants) wired as the FIRST design authority so it auto-loads; re-read it at chunk start and before 'done', asking: serves the soul? what's still UNWIRED? did I hang the sign (navigable by someone who doesn't know the system)? Built != done.
- [#verify][#web][#infra] [I:7] Built a zero-dep CDP browser-driver (~/.hermes/scripts/cdp_drive.py, stdlib WebSocket) so I can verify web INTERACTIONS (real click -> observable DOM effect) not just static renders — proved it by real-clicking the tutor's Neuro button and watching the panel open -> GOTCHA on the 2.78GB box: my first close() killpg'd the launched proc but a headless-chromium CHILD escaped the process group and LINGERED (~400MB, not freed) -> harden close() to: (1) fire-and-forget Browser.close (graceful full-tree exit, don't wait for a reply it won't send), (2) killpg TERM then KILL, (3) BACKSTOP reap any straggler by the driver's UNIQUE --user-data-dir=/tmp/cdp-prof via pgrep+kill-by-PID (safe: pattern only matches the driver's own chromium, never his ChromeOS browser; pgrep excludes itself so no self-match). RULE: launch-drive-KILL a browser on this box always needs a unique-profile reap backstop; killpg alone leaks a child. Honest boundary: drives the Linux Chromium, NOT his ChromeOS browsing Chrome.
- [#verify][#web] [I:8] Headless chromium + WebGL OOM-killed my shell (proxy-service cgroup cap), orphaning 7 chromium each try -> death spiral -> RULE: on the 2.78GB box verify WebGL with an ISOLATED low-mem harness (no models/loop) + chromium --headless=new --single-process --virtual-time-budget=N --screenshot=out.png (self-exiting, writes PNG before any OOM, shell survives); never CDP-drive the full game headless.
- [#sector:game] [#verify][#web][I:7] headless WebGL capture on the 2.78GB box kept hanging at chromium launch -> THREE compounding causes: (1) each timed-out CDP run leaks a 5-proc chromium tree + a /tmp profile (50 junk dirs accumulated) that degrades the debug endpoint; (2) hardcoded port/profile made runs collide; (3) I widened the endpoint-poll loop range(60->160) and each urlopen(timeout=1) blocks ~1s so __init__ ran up to 200s and was always killed mid-launch -> FIX: unique port+profile per pid, startup-reap orphans + rm tmp junk, keep the poll loop small, and run the capture as a BACKGROUND job with its own timeout+markerfile so the 120s tool limit can't cut it mid-run and hide the real error.
- [#sector:game] [#dispatch][#process] [I:7] claude -p doers kept dying ~2min in, work half-done -> the Bash TOOL auto-backgrounds at its 120s default and backgrounded doers die at turn end (the 'timeout 590' shell prefix does nothing about that) -> for a blocking long doer, set the Bash tool timeout param high (up to 600000ms) so it runs in-foreground to completion; still tell it to commit per-item so a real overrun loses nothing
- [#sector:game] [#verify][#web] [I:7] 'FBX renders on its side' report + Z-up prior -> rendered raw import in the harness: model was ALREADY upright, the symptom was a yaw (front=-X, player always saw it broadside) -> render the raw asset at rot 0 BEFORE applying any axis-correction; a prompt's stated root cause is a hypothesis, not a measurement
- [#sector:game] [#process][#shell] [I:6] Headless chromium wedged 17min on a 9s virtual-time budget -> a stale duplicate agent session was running its own chromium on the 2.8GB box (memory contention; no renderer process ever spawned) -> before long headless renders, pgrep for rival claude/chromium sessions and kill stale duplicates; a shot that outlives its virtual-time budget several times over is wedged, kill and rerun
- [#sector:game] [#process] [I:8] Two identical claude -p dispatches ran the SAME task concurrently in one checkout (overseer re-dispatch while original still alive) -> both editing web/index.html = interleaved uncommitted state, near-corruption -> BEFORE first edit of a shared file, pgrep for a sibling claude -p on the same task/dir; if one is alive, stand down to verifier role instead of editing
- [#sector:game] [#dispatch][#process] [I:7] a resent user msg made me launch the SAME long claude -p dispatch twice -> two agents raced on one file, one stuck + wasted RAM on the 2.78GB box -> BEFORE launching a long doer, pgrep for an already-running 'claude -p' on that project; if one exists, attach/wait, don't spawn a rival. And pkill -f can kill my own shell (cogito-guard blocks it) -> kill by PID.
- [#sector:game] [#shell][#verify][I:7] kill $(pgrep -f '<pat>') killed my own shell twice (exit 144) -> pgrep -f matches the FULL command line, and the bash -c wrapper running the kill CONTAINS the pattern -> never interpolate pgrep -f into a kill in the same command; get PIDs in a prior separate call, or match on the binary path only via pgrep -x/fuser -k on the port.
- [#sector:game] [#deploy][#verify][I:6] Four halo-x-brainrot 'vercel --prod' runs all sat at status UNKNOWN with no build, while the prior day's identical deploys went Ready in 3-6s -> the stall is Vercel-side queueing, NOT the CLI/auth (vercel whoami exits 0) and NOT slow upload -> diagnose a silent deploy with 'vercel ls <project>' (Status/Duration columns) BEFORE re-running; repeated --prod calls just queue more UNKNOWNs.
- [#sector:game] [#deploy][#verify] [I:6] npx vercel --prod --yes piped to '| tail -5' produced ZERO output for 24 min and never deployed -> tail buffers the whole stream so progress is invisible, and the piped npm exec stalled silently before ever printing 'Deploying' -> NEVER pipe a long deploy through tail/head; redirect unbuffered to a log file (> /tmp/x.log 2>&1) and poll the log + the live URL
- [#shell] [I:7] 'for p in $(pgrep -f "npm exec vercel"); do kill $p; done' exited 144 twice — pgrep -f matched the bash -c command line RUNNING the loop, so it killed our own shell -> ALWAYS bracket one char to break self-match: pgrep -f 'npm exec ver[c]el'
- [#sector:game] [#game][#verify] [I:6] Skybox moon read as a dark hole from spawn -> the key light sat BEHIND the spawn view so no reposition inside the visible frustum could ever show a lit face (phase geometry, not placement) -> before moving a 'badly lit' skybox object, dot the view->object and light directions; if the lit face is geometrically unreachable, bake the phase into the texture and go unlit
- [#sector:game] [#git][#process] [I:7] My uncommitted edit got swept into a PARALLEL session's 'git commit -am' under an unrelated message, and both sessions fixed the same defect two different ways -> two agents sharing one working tree have no isolation -> before editing, git status + git log -3 to detect a concurrent session; commit immediately after each edit; if a twin already landed the fix, reconcile (keep one approach, revert the stray) instead of stacking both
- [#sector:game] [#process][#verify] [I:7] Dispatched claude -p doers under 'timeout 590' and got exit 143 + ZERO output — claude -p buffers its whole reply until exit, so a timeout-kill loses the entire report even though per-item commits survived -> BUILT ~/.hermes/scripts/dispatch.sh: streams --output-format stream-json to a log (partial work readable after a kill) + heartbeats to phone + a bash EXIT/TERM trap that ALWAYS reports verdict (DONE/TIMED OUT/KILLED) AND the commits that landed (git BEFORE..AFTER). Silence is now structurally impossible. RULE: never run a long claude -p bare/piped-through-tail; always via dispatch.sh.
- [#process][#boundary] [I:7] In self-directed sandbox 'making' I assumed a toy name was novel and Write'd reservoir/core.py from scratch -> a superior reservoir ESN toy already existed (Jul 15); only the Write 'must Read first' guard stopped a clobber -> before building a NEW sandbox toy, ls the target dir FIRST; if it exists, switch to IMPROVE (add a standalone script, never overwrite the verified core).
- [#verify] [I:6] Bloom-filter toy: empirical fill/FP drifted 3-6% from the exact formula -> I'd used double-hashing (k positions from an arithmetic progression) which CORRELATES the k bit positions and breaks the independence the p=f^k theory assumes -> when a toy must match an exact closed-form, use k INDEPENDENT (salted) hashes not the double-hashing speed trick; verify the mechanism against the MEASURED intermediate (fill), not only the idealized end formula.
- [#verify] [I:6] A stale hardcoded truth-constant (STAT0 0.352 vs true 0.387) made a Monte-Carlo convergence slope read FLAT (-0.005) -> the fixed offset dominated 'error' so it couldn't shrink -> when a convergence/scaling fit comes out flat, suspect a WRONG truth value before a broken estimator; compute the truth in-code (power iteration) instead of pasting a constant.
- [#verify] [I:7] Fisher-Yates shuffle came out biased (secretary MC read 42% vs true 37%, outside 4 sigma) -> drew the bounded index with LCG next_u64() % n, and an LCG's LOW bits are non-random (low bit just alternates) -> for a bounded integer from an LCG use the HIGH bits: floor(uniform()*n), never % n; shuffle-in-place can MASK the bias, reset-each-trial exposes it.
- [#network][#verify] [I:6] memory-freshness URL check hung >120s then cried wolf on 3 benign URLs -> urllib timeout doesn't bound DNS, and naive liveness flags placeholders/method-gated-405/local hosts -> use 'curl -m' (hard wall) concurrently for batch liveness, and filter false positives (placeholder tokens, 400/401/403/405/429=alive, *.linux.test/localhost) or a watchdog gets ignored
- [#verify][#sandbox] [I:7] A browser-side AC-attenuation measurement read 2.26 vs 3.62 theory, and the slow case was also biased low -> the fit region still held the unsettled DC transient (settled only 1.5 tau), and that transient decays at k=1 per lambda, dragging the fitted decay constant TOWARD the DC value -> when a measured rate is biased toward the value of a competing slower mode, suspect insufficient settling, not the solver; settle ~10 time constants and re-measure.
- [#verify] [I:7] A PDE check showed 1.037 then 20.2% error vs the analytic Green's function -> BOTH were faults in my TEST, not the code: first a factor-of-2 (a reflecting boundary halves the CHARGE on a half-line, not the VOLTAGE), then the far end's mirror image genuinely arriving -> when numerics disagree with a closed form, compute the boundary's image contribution first; if it explains the gap to 3 digits, strengthen the test to compare against the image sum rather than restricting the domain.
- [#shell][#verify] [I:7] Added a module-level helper to hermes_autobuild.py that called hermes_usage.reset_epoch(); ast.parse said OK but every real cycle would have NameError'd -> the script lazy-imports hermes_usage/hermes_queue INSIDE main(), so module-scope functions never see those names -> when adding a top-level helper to a script that imports lazily, import the dep inside the helper AND exercise it by loading the module standalone; a syntax check cannot catch a scope error
- [#shell] [I:7] Used pgrep -f 'pat' + kill to stop a background job and killed my own shell (exit 144) -> pgrep -f matches the FULL command line, and the bash -c wrapper running the pgrep CONTAINS the pattern, so the shell matches itself — the same trap as pkill -f, not avoided by switching to pgrep -f -> RULE: exclude self explicitly (pgrep -f pat | grep -v $$) or match on a pattern that cannot appear in the killer, e.g. pgrep -f '[v]erify_selftest', or kill the known PID captured at launch.
- [#verify] [I:8] Built a PK teaching tool where answer keys came from closed-form formulas -> a key checked by the code that produced it is not checked at all; the gate only means something if the second path shares NO code -> RULE: pair every analytic solution with a numerically-integrated twin (RK4 on the raw ODE) and diff them over randomised params — it caught 3 real defects here (float drift adding a spurious dose, a fixed 40-cycle warm-up that under-converged when tau<<t-half, and a key derived from an unrounded value the student never sees). Also: state answer_value in the units actually PRINTED, or the consistency check compares 0.938 to '93.8%' and fails on formatting.
- [#verify] [I:8] Randomized closed-form-vs-numeric gate PASSed on the default seed but FAILED on another (0.7% off, 4 checks) -> the numeric reference used a HARDCODED warm-up (60 dosing intervals / 40 cycles) which only reaches steady state when tau is short vs half-life; for long-t½/short-tau it stops ~99.4% converged and reports a CORRECT formula as wrong (and the same hardcoded budget inside pk.py was quoting under-converged numbers into real answer keys) -> derive iteration budgets from the system's own time constant (n >= ln(1e5)/(k*tau)), never a magic constant, and ALWAYS run a randomized gate over several seeds before believing a PASS — one seed does not sample the bad regime.
- [#verify] [I:7] Selftest 'exit 124' read as a broken gate -> 124 is timeout(1)'s signal, not a test failure; the suite passed but took 95s against a 60s command timeout -> on a nonzero exit from a gate, read the CODE first (124=timeout, 137=OOM-kill) before debugging the logic; then fix the runtime, not the math.
- [#config][#verify] [I:8] Hermes silently switched to gemini-3.5-flash mid-session and hung on 180s stale streams -> --effort max made Opus generations exceed the Hermes CLIENT's ~15min request timeout (the proxy's own timeout was already ~infinite at 22162140000ms, so it was innocent); 3 APITimeoutErrors then triggered a silent per-request fallback -> when a model 'falls back', read the log for the FIRST error_type before theorising (APITimeoutError != quota != context limit), and match effort level to the client timeout; a fallback that swaps brains without telling you is worse than a hard failure.
- [#process][#verify] [I:7] Pharmakon sat unused for a day despite passing 868 self-tests -> a finished engine with no delivery path is not shipped; its only doors were an HTML file on the box and an interactive terminal drill he cannot run -> RULE: for any tool built FOR Eduardo, the definition-of-done includes a door he can open from his phone (Telegram pipe), not just a working core
- [#verify] [I:6] pharmakon spec (TIERS-SPEC) and every textbook quote occupancy-at-half-effect as 1/(tau+1), but deriving it from EC50=KA/(tau+1) gives 1/(tau+2) -> a spec/slogan is not a derivation; when a doc and an algebraic derivation disagree, let the independent numerical path settle it and write the discrepancy into the lesson instead of silently matching the doc
- [#shell][#process] [I:8] A long driver launched with nohup INSIDE a background Bash tool call died after one iteration -> the harness reaps the background task's whole process group on completion, and nohup only ignores SIGHUP, it does not detach from the group (the grandchild doer survived only because dispatch.sh had already re-parented it) -> RULE: run a long-running loop AS the background command itself (run_in_background on the driver), never nohup-and-return from a short wrapper; if a wrapper is unavoidable use setsid
- [#critical][#process][#verify] [I:9] An agent's child dies with — or outlives — the wrong thing (compiles 3 raw) -> three faces of one error: nohup and the harness background slot both still hang off the session, so multi-hour work died within minutes; stopping a service kills its WHOLE cgroup, so an agent upgrading its own host dies mid-write with no rollback; and a killed dispatch WRAPPER does not kill its 'claude -p' CHILD, which kept committing and made a stated HEAD stale enough to nearly clobber an 80KB file -> RULE: launch anything that must outlive the session as systemd-run --user --unit=X --collect --setenv=PATH=... (user units do NOT inherit ~/.local/bin); for self-surgery confirm the separate cgroup with systemctl show -p ControlGroup and notify over a channel that does not depend on the thing being restarted; before any dispatch run pgrep -af 'claude -p' AND git log -1. Verify liveness by the observable the work should move, never by 'I launched it' — and never read a wrapper's death as the doer's.
- [#verify] [I:7] A timed-out agent left pk.py/numeric.py math complete but the rung dead -> the missing work was WIRING (generator, registry, curriculum flag, lesson, self-test branch), not modelling -> before rebuilding a 'failed' node, diff what already landed; run the existing gate first to get a baseline count, then only add what is absent.
- [#git][#process] [I:7] Mid-task 'git status' showed my numeric.py edits as UNMODIFIED and pk.py as only 13 lines changed -> a SIBLING agent working in the same checkout had run 'git commit -a' and swept my in-flight files into its own unrelated commit -> RULE: when parallel agents share one working tree, never assume your uncommitted work is still uncommitted; before committing, diff HEAD for your own files, and stage by explicit path rather than -a so you do not sweep up theirs in return.
- [#verify] [I:7] Worksheet printed the same number twice as two different 'named errors' -> distractor distinctness was checked on FLOATS (rel-tol) while the sheet shows 3-sig-fig STRINGS, so 1.9999 and 2.0 collided in print -> RULE: guard generated multiple-choice options on the RENDERED string, not the underlying value; and if a named error is structurally degenerate for the whole parameter range, replace it with a different named error rather than redrawing forever.
- [#verify] [I:7] Generator loop hung with no output -> a while-True redraw required _exact3() on a value DERIVED from other draws (fe*ln2*V/t_half), which almost never survives 3-sig printing, and fmt() also emits '1,500' which float() cannot parse -> RULE: only require print-exactness on values the student COMPUTES FROM (drawn inputs), keep them under 1000, and never on the answer.
- [#verify] [I:7] A displacement rung's independent check would have been a nested RK4 march costing ~1s per key -> the check must be independent, not necessarily an INTEGRATION: a steady state is a raw equilibrium, so bisecting R = Q(C - C_out(C)) with Newton on the binding equilibrium inside is both independent and ~50x cheaper -> RULE: for steady-state claims use root-find-on-the-raw-balance as the working check and keep ONE RK4 march of the same ODE in the self-test to prove the fixed point is actually approached.
- [#verify] [I:8] Wrote a closed form AND its teaching prose in one pass; the formula was right but I asserted its limits backwards (claimed a pure prodrug loses active-drug exposure in proportion to enzyme activity when the true limit is NO change) -> the algebra was checked numerically but the PROSE around it never was -> RULE: for every closed form, write the limit cases as explicit numeric assertions (f(x->1)==?, f(x->0)==?) in the gate, not just the main identity — the limits are where the intuition lies and they are free to check
- [#process][#verify] [I:7] SOUL.md ordered me to 'fill the Dispatch Template (TASK/CONTEXT/SCOPE/CHECKPOINT/VERIFY/REPORT)' but no such template existed in the claude-code skill -> a persona/config instruction that names an artifact is not proof the artifact exists, so every dispatch silently improvised the fields -> RULE: when a config references a NAMED artifact (template, script, section), grep for it before trusting it, and build it if the grep is empty
- [#verify][#network] [I:8] A daily PubMed watcher looked healthy for weeks but never reported the NEWEST papers -> esearch sort=date is not a real schema; PubMed answers 'Unknown sort schema date ignored' inside warninglist and silently falls back to RELEVANCE order, so a small retmax returns the same top-cited papers of the window forever (correct value: sort=pub_date) -> RULE: when an API takes a sort/filter/mode string, read the response's warning/message field and treat any 'ignored' as a hard error; a silently-dropped parameter is a broken query wearing a 200 OK.
- [#process][#verify] [I:8] Upgrading a FORK 2 releases behind looked like a 16k-commit merge nightmare -> merging upstream INTO the fork hides which local patches are still needed; cherry-picking the fork's commits ONTO the new upstream tag makes git report already-merged ones as empty commits -> RULE: rebase-onto-tag, not merge-into-fork; 7 of 8 local patches turned out to be upstream already, leaving 1 real carry
- [#verify][#process] [I:7] hermes-briefing/curiosity showed red 'failed' for 3 boots and I first read it as a broken service -> TWO bugs stacked: (a) After=hermes-gateway.service is satisfied when a Type=simple unit EXECS, but the real dependency was its CHILD bridge ~35s later, so the ordering guarded the wrong thing; (b) send.py had built a _last_durable contract so queued!=failed, but all 18 callers still did sys.exit(0 if ok else 1) on delivered-ness -> RULE: After= on a Type=simple unit orders against the process, NOT its readiness -- gate on the observable (poll the port) instead; and when you fix a contract in a shared lib, grep every caller for the old pattern, a lib-only fix is not landed. Verified: dead-bridge probe -> durable()=True -> exit 0; live bridge -> ready in 0s; 5/5 queued msgs drained.
- [#critical][#verify] [I:9] A checker that cannot fail reports success identically to a correct one (compiles 2 raw) -> a readiness gate on HTTP 200 released 9s early because /health returns 200 the instant the socket binds while carrying {status: connecting}, and prior art gating on '"connected"' already sat unread in the same repo; separately a 33-problem audit scored 33/33 on its FIRST run and a 4000-problem gate passed every family first try — evidence about the CHECKER, not the answer key -> RULE: before believing any verifier, POISON it — perturb one digit of every real answer and require rejection (225 x accept-real AND reject-mutated is what makes 33/33 mean something), in the SAME session you write it. Grep for an existing checker before hand-rolling a weaker second one. 'Service responds' is never readiness for a session-proxying service: gate on the payload's own state field and reproduce the real failure condition — component tests passing is not evidence.
- [#config][#network][#sector:infra] [I:8] Hermes kept 'falling back to Gemini' with the claude-proxy service up and healthy -> Hermes never called the proxy at all: config.yaml model.base_url=http://127.0.0.1:8788 is IGNORED by the credential-pool path, which auto-seeded a borrowed 'claude_code' OAuth entry and sent traffic to https://api.anthropic.com; Anthropic now answers third-party-app OAuth with HTTP 400 'Third-party apps now draw from your extra usage, not your plan limits', which trips the configured fallback chain -> gemini (whose free tier was already 429). RULE: to pin Hermes to the local proxy set BOTH ANTHROPIC_BASE_URL=http://127.0.0.1:8788 and a non-placeholder ANTHROPIC_API_KEY in ~/.hermes/.env (empty string and the literal 'placeholder' both fail has_usable_secret); config.yaml base_url alone is not enough. Diagnose with 'journalctl --user -u hermes-claude-proxy' turn count (zero turns = bypassed, not broken) + grep 'Fallback activated' in ~/.hermes/logs/agent.log.
- [#shell] [I:8] cogito-guard blocked 'pkill -f canvas_server.py', so I 'obeyed' by switching to pgrep -f + kill-by-PID — and still died with exit 144 -> the guard's real point is the PATTERN, not the tool: -f matches the FULL command line, and my own shell's command line CONTAINED the literal string canvas_server.py, so pgrep listed my shell's PID and I killed myself; swapping pkill for pgrep changed nothing -> RULE: any pgrep -f/pkill -f pattern must be written so it cannot match the command that contains it — use a bracket class (pgrep -f 'canvas_serve[r]\.py') or match on a distinguishing prefix; kill-by-PID is NOT the fix, a self-excluding pattern is
- [#process][#git][#ai-tutor] [I:8] Dispatched to build MAP_PLAN step 4 with 'HEAD is 740d6eb', but git log showed TWO newer commits (4a,4b) and a live uncommitted diff; ps proved a second claude -p doer (PID 12882, 'Build steps 4 and 5') had started 82s before me on the SAME 80KB single-file PAGE string -> Hermes dispatched two overlapping doers for one task and neither prompt declared the other; a stated HEAD in a dispatch is a SNAPSHOT, not a lock -> RULE: before the first edit in a dispatched build, run 'git log --oneline -3' + 'git status' and compare to the HEAD the prompt asserts; if they disagree, run 'ps -o pid,lstart,args' for other 'claude -p' doers in the same workdir BEFORE editing. Two agents on one file = guaranteed clobber. Yield to the earlier-started doer with the larger scope and surface the collision; never race. Also: 'pkill -f canvas_server.py' from inside a dispatched agent is unsafe — it can kill a PEER agent's server; kill your own orphan by PID.
- [#verify][#web] [I:8] cdp_drive.py died with BrokenPipeError on every eval after the tutor map rendered, 3x reproducible -> I nearly reported it as a browser-crashing page defect, but the SAME headless chromium run standalone with --screenshot (which paints and composites) exited 0 and produced a correct 429KB render -> RULE: a CDP-session broken pipe is evidence about the DRIVER, not the page. Discriminate first with standalone chromium --screenshot (paints) vs --dump-dom (no paint); only call it a page defect if the standalone paint also fails.
- [#verify][#shell] [I:7] A browser-verify loop kept reporting the page as EMPTY (button missing, 0 nodes) and I nearly filed it as a product defect -> two environment artifacts, not bugs: (1) the tutor server launched as '( python3 srv.py & )' inside a Bash tool call is reaped when that call's process group ends, so it was DEAD by the next tool call and the browser was evaluating a failed load; (2) a stray /tmp/bisect.py left by an earlier doer SHADOWED the stdlib bisect module because running a script from /tmp puts /tmp on sys.path, breaking 'import urllib.request' inside the CDP driver -> RULE: (a) any server that must outlive the tool call that starts it goes in systemd-run --user --unit=X --collect (the ( & ) subshell is NOT detachment); (b) before believing a browser check that reports an empty DOM, assert the server is LISTENING in the same call; (c) never run verify scripts from /tmp — keep them in a clean dir (~/.hermes/verify)
- [#process][#verify] [I:8] A dispatch reported 'FAILED (exit 137)' and 'no commits', so I treated the doer as dead and started my own verify runs -> the OOM killer had killed the dispatch WRAPPER, not the claude -p child: PID 18150 was still alive and landed 3 more commits while I worked, and it held port 9007 and ~600MB, so my parallel server+chromium runs collided with it and died in ways that looked like a product bug -> RULE: a dispatch verdict describes the WRAPPER only. Before re-dispatching or verifying after any FAILED/KILLED/TIMEOUT verdict, check for a surviving doer (ps -eo pid,comm | grep claude, read /proc/PID/cmdline) and check the port and RAM it holds; on this 2.78GB box never run a second server+browser while a doer is live
- [#shell] [I:7] A CDP harness at /tmp/bisect.py failed with 'circular import: cannot import name CDP' and leaked a chromium tree -> the filename shadowed the stdlib 'bisect' that urllib.request imports, and /tmp (the script dir) sits on sys.path ahead of it; a later rm never ran because an earlier command in the same chain failed -> RULE: never name a scratch script after a stdlib module (bisect, code, types, select, signal, token); prefix scratch harnesses t_/verify_, and reap chromium by unique --user-data-dir in a finally block, never as a trailing command in a && chain
- [#verify][#web] [I:7] After retinting a toy, its stat panel read 0/'—' and another toy's canvas was blank, both of which looked exactly like my change had broken the render -> re-rendering the UNMODIFIED original under the identical headless budget showed the same 0 and the same blank grid: they were the toys' own states, not a regression -> RULE: before attributing a visual anomaly to your change, screenshot the untouched original under IDENTICAL conditions (same virtual-time budget, same window size); a before-shot taken at different settings is not a control
- [#verify][#web] [I:8] Twice concluded the tutor map 'did not work' when it did: my probe counted '#log .msg' (the chat log is #msgs) and selected '.jnode[data-domain=math]' (data-domain sits on the parent .mlane, not the node) -> both selectors returned 0 on a WORKING page, which reads identically to a broken feature -> RULE: before reporting a UI check as failed, prove the SELECTOR resolves on a known-good state (assert querySelector(sel) is non-null, or dump-dom and grep the real attribute); a zero count is only evidence once the selector itself is verified
- [#process][#ai-tutor] [I:7] Filling the tutor map from 3 to 12 cross-domain threads made the web MORE connected and LESS readable: all threads route through one gutter, so 12 near-identical dashed verticals bundle into a tangle no eye can trace -> a link view that is legible at N=3 is not automatically legible at N=12; edge COUNT and edge READABILITY scale in opposite directions -> RULE: when adding edges to any graph/map UI, re-render and LOOK at the target density before calling it done, and budget a disambiguation mechanism (hover-dim, per-thread colour, curved bundling, or show-on-select) as part of the fill work, not as a later polish
- [#web][#verify] [I:8] Thread-select looked right in the screenshot (partner ring visible, others dimmed) but --dump-dom showed 'act paths: 0' — nothing was actually promoted -> layoutEdges() rebuilds svg.innerHTML from scratch on a later rAF, so classes applied to PATHS at click time were destroyed, while classes on the NODE elements survived; the half-working result was visually plausible -> RULE: when a render function rebuilds innerHTML, any selection/highlight state on its children must be REPAINTED after every rebuild, not applied once at event time; and verify such state by dump-dom counting the class, because a partially-applied highlight looks correct in a screenshot
- [#sector:web][#web][#critical][#verify] [I:9] Tailwind v4 fails SILENTLY three ways (compiles 3 raw, two of them the same cascade bug found twice on jlpaintingsc.com) -> (a) arbitrary values are ambiguous: text-[var(--x)] compiles to color: not font-size, bg-[linear-gradient(...)] emits no rule at all; (b) an invalid var() is 'invalid at computed-value time' so colour falls back to INHERITED rather than being ignored (dark-hero H1 inherited body ink at 1.32:1); (c) any UNLAYERED element rule (a{color:inherit} in globals.css) beats @layer utilities at any specificity, silently killing every colour utility on every <a> sitewide -> RULE: write text-[length:...] and bg-[image:...] explicitly; keep every bare element rule INSIDE @layer base; never trust the chain class-in-HTML -> rule-in-CSS -> painted pixel: grep the built .next/static CSS for the compiled declaration, diagnose a matched-but-losing utility with CDP CSS.getMatchedStylesForNode, then sample the pixel (convert -crop ... histogram:).
- [#sector:web] [#process][#verify] [I:8] A weekly agent fired on schedule for a month and 12 toys still piled up unshipped -> the timer was healthy; its MESSAGE was the defect. It sent Eduardo a bare count plus CLI commands to run himself, but he does not use the CLI (Hermes drives tools for him), so the human step was assigned to someone structurally unable to take it. A half-done 'approved but never published' record was the tell: someone did step 1 of 3 and stopped -> RULE: when auditing a stalled pipeline, check WHO the pending action is assigned to before assuming the scheduler broke — journalctl the unit FIRST (a firing timer exonerates the schedule and indicts the payload). Fix by collapsing the whole chain into one command the AGENT can run, and make the notification request a REPLY rather than a terminal session; add a staleness escalation so 'ignored for a month' cannot render identically to 'nothing to do'.
- [#verify][#web] [I:7] A real CDP Input.dispatchMouseEvent click on a slider did nothing and the check FAILED (index 3 -> 3) -> the control sat at document y=5781 on a 7,544px-tall page inside a 2,400px window, and getBoundingClientRect is VIEWPORT-relative, so the computed coordinate pointed off-screen -> RULE: before any CDP mouse click, scrollIntoView({block:'center'}) the target, sleep, and only THEN read the rect; on a long page a coordinate click silently misses and a weak assertion will pass vacuously, so gate the follow-up state check on the control having actually moved.
- [#verify][#shell] [I:8] grep in the Bash tool returned EMPTY for patterns that provably exist in the file (scrollIntoView, import, even grep -c '') and exited 1 -> a silent grep failure is indistinguishable from a real absence, and I nearly reported a required fix as MISSING -> RULE: when a grep for a string you strongly expect returns nothing, do NOT conclude absence — re-check with python3 open().count() before reporting; a negative grep is only evidence if a positive control grep on the same file works.
- [#sector:web] [#shell] [I:8] pgrep -f/pkill -f killed my own shell twice (exit 144) even using the bracket trick 'nex[t]-server' -> the pattern TEXT itself appears verbatim in the invoking shell's /proc cmdline, so ANY -f pattern self-matches; the bracket trick only defeats grep-on-ps, not pgrep-on-own-cmdline -> to kill a dev server, resolve the PID from the LISTENING PORT (ss -lptnH 'sport = :PORT' | grep -oP 'pid=\K[0-9]+') and kill that PID; never pgrep -f for a process whose name you just typed.
- [#sector:web] [#verify][#shell] [I:8] The prescribed audit grep "grep -rn 'text-\[var(--' | grep -v 'color-'" returned NOTHING while the exact defect sat on a matched line -> the same className also contained text-[var(--color-white)], so the line-oriented 'grep -v color-' filtered out the whole line and hid md:text-[var(--text-h2)] -> RULE: when auditing for a defective TOKEN, extract tokens not lines (grep -rhoP '(?:[a-z0-9:\[\]-]+:)?text-\[[^\]]+\]' | sort -u) — a line-level -v filter on a multi-utility className is a false-negative generator.
- [#sector:web] [#verify][#web] [I:7] Two silent-failure traps when auditing a site headlessly: (a) getComputedStyle returns Tailwind opacity utilities as oklab(...), and a naive rgb parser read '0.999994' as the R channel, producing 165 fabricated contrast failures; (b) ImageMagick refuses images over the 16KP policy limit and 'identify'/'convert' fail with NO usable error, so a 16180px-tall mobile fullPage screenshot silently produced nothing -> RULE: resolve any CSS colour to sRGB by painting it into a 1x1 canvas and reading the pixel (handles oklab/color-mix/currentColor), composite translucent ancestor backgrounds before computing ratio; and tile tall mobile captures into viewport-sized shots instead of one fullPage PNG.
- [#verify][#process] [I:8] A Levy-stable panel drifted off its own exact limit law as n grew -> the Box-Muller generator cached the second normal of each pair as a spare, and 1/(r cos t)^2 and 1/(r sin t)^2 are DEPENDENT, so consecutive summands violated the i.i.d. precondition of the very theorem under test -> RULE: when a study tests a theorem, audit the RNG against that theorem's own preconditions first; and confront distributions with an exact CDF statistic, not a histogram by eye — the eye passed this, the KS number did not.
- [#verify] [I:7] A driver check that tried to prove a NEGATIVE ('the page nowhere claims proof of agreement') failed on the page's own sentence DENYING that claim -> a phrase-grep cannot distinguish a claim from its disclaimer -> RULE: never assert absence of a phrase; assert presence of the disclaimer you actually want. Also: textContent keeps HTML source newlines+indent, so any asserted phrase must not straddle a line break.
- [#verify][#web] [I:6] Edited three verified single-file pages to add cross-links -> a link/CSS edit can still break a page's own headless driver (prose and DOM-count assertions) -> after ANY edit to a page that owns a drive.mjs, re-run that driver before calling done; a disk-level link check proves the target exists, not that the page still passes.
- [#verify][#process] [I:8] A verification driver failed on two 'physics' checks that were actually fine -> both assertions used hand-written tolerances against quantities with a KNOWN error floor (a 3-point Laplacian's O(h^2) discretisation, and the sampling s.e. of a mean over R samples), so the bound was testing the author's nerve not the system -> RULE: never assert a discrete/sampled quantity against an exact constant with a guessed epsilon; assert the RATE (refine and measure the convergence order) or the Z-SCORE (error bar measured off the sample). Both convert a guess into a second exact number.
- [#sector:web] [#web][#verify] [I:7] Auditing site copy for unverifiable claims from content.json ALONE misses the worst offenders -> the strongest claim on jlpaintingsc.com ('5-Star Rated') was hardcoded in app/opengraph-image.tsx, invisible to the CMS and to any JSON grep, and a false visualizer privacy note lived in content.json but was already overridden in the component -> RULE: a claims audit must cross three sources - the JSON, a grep of app/ + components/ for literal claim words, and the LIVE fetched HTML - because JSON-only over-reports (dead fields) AND under-reports (hardcoded TSX + build-time OG images) in opposite directions.
- [#sector:web][#verify] [I:8] A gallery toy shipped as a dead static shell with a blank canvas and ZERO console errors -> its index.html was truncated: it ended mid-file with no closing </script></body></html>, and an unterminated <script type=module> at EOF is never executed by Chromium, so nothing throws -> RULE: 'no uncaught errors' is not evidence a page ran. Probe for POSITIVE evidence the page's own JS executed (canvas ink, rAF called, a DOM value the script overwrites differing from the HTML default), and scan generated HTML for open/close <script> count mismatch and a missing </html>.
- [#sector:web] [#verify][#tooling] [I:6] My synthetic CDP mouse clicks at getBoundingClientRect centres reported 'sandpile still blank after pouring grains' -> the controls sat below the 900px headless viewport so the coordinates missed, and a real in-page element.click() proved the toy works -> RULE: coordinate-based clicks are only valid after scrolling the element into view; for a pass/fail verdict prefer element.click() and cross-check any negative with a positive control before calling a toy broken.
- [#sector:web] [#web][#verify] [I:6] A claims-audit doc's own line numbers had drifted (before/after subtitle listed 6 pages, grep found 5; Visualizer.tsx:806 was really 809) -> a prior audit is a SNAPSHOT of a moving tree, and a proposal that quotes it inherits its rot -> RULE: when drafting exact diffs from an earlier audit, re-grep every quoted string and assert every '-' diff line exists byte-for-byte in the source before publishing; a diff with a stale anchor silently fails to apply.
- [#verify][#process] [I:7] A --dry-run of hermes_autobuild wrote real state.json and a real log entry -> DRY only gates the dispatch, not the checkpoint writes at the end of main() -> RULE: before probing any cycle-runner with --dry-run, back up state.json OUTSIDE the run and restore it after; a dry-run that still checkpoints is not a dry run.
- [#sector:web] [#verify][#web] [I:6] Headless draw-probe reported Plates II and IV as 0 lit canvases and nearly logged them as dead toys -> both plates gate rendering on scroll (IntersectionObserver), and the probe never scrolled — same class as the sandpile below-the-fold false negative -> RULE: a canvas 'blank' verdict is only valid after scrolling the page top-to-bottom first; scroll, then probe, and treat any all-zero result on a long page as a probe defect until disproved.
- [#sector:web] [#critical][#verify] [I:10] Hermes autobuild did zero discretionary work for weeks while the timer fired perfectly -> its own safety check manufactured the evidence that blocked it: hermes_usage.check() reads the meter via 'claude /usage', which WRITES a session transcript into ~/.claude/projects/, and recent_activity() globs that same directory to decide if Eduardo is active — 43 of 43 transcripts written 02:00-07:00 while he slept were our own probes -> RULE: when a guard reads a signal your own process also WRITES, it will deadlock itself; before blaming a scheduler, journalctl the unit (a firing timer indicts the payload) and ask WHO wrote the evidence the guard is reading. Filter self-authored artefacts out of any liveness/idleness check.
- [#sector:web] [#verify][#web] [I:7] Headless sweep reported 82 consecutive pages as blank with zero canvas ink -> Chromium itself had died at page 20 and every later 'result' was a dead-browser timeout, not a page verdict -> RULE: a browser/harness-level failure is a PROBE DEFECT, never a dead-toy verdict; relaunch the browser every N pages, retry any page whose failure was the harness, and treat a run of identical all-zero results as harness death until disproved.
- [#sector:web] [#verify][#web] [I:6] toy-snowflake and toy-sandpile probed as blank canvases after an element-level click sweep -> the sweep clicked EVERY control in DOM order and happened to end on 'Reset', wiping what the earlier clicks drew -> RULE: element-level clicking fixes coordinate-click false negatives but introduces its own; confirm any blank verdict by driving the ONE control that makes the toy draw (Grow, +10,000 grains) and never let Reset/Clear be the last click before a probe.
- [#verify] [I:8] A correctness gate that prints PASS proves nothing on its own — a checker branch that silently no-ops or throws-into-a-pass looks identical to a real one -> ROOT CAUSE: the gate only ever sees CORRECT keys, so it never exercises its own rejection path -> RULE: after adding any checker branch, run a MUTATION positive control: corrupt every generated answer by one digit and require 100% of the mutants to be flagged. Report the mutants-caught count next to the pass count; a pass without it is unearned.
- [#process][#verify] [I:8] A queued autobuild task silently retried forever, holding the lockfile and starving the queue for hours -> subprocess.run(timeout=) raised TimeoutExpired which propagated out of dispatch() BEFORE the day-log write, so the killed cycle left no log entry at all (visible only in journalctl) and the task stayed status=pending, so the next cycle dispatched the same oversized task again -> RULE: a worker timeout must be CAUGHT and recorded where the operator looks, never allowed to unwind past the logging; and any queue that re-dispatches on non-completion needs the failure written back to the item, or 'too big to finish' renders identically to 'not started yet'.
- [#shell][#process] [I:7] Tried to stop a background self-test with pgrep -f 'python3 verify_selftest' inside a for-loop kill; the shell exited 144 and killed my own background jobs -> pgrep -f matches the FULL command line, and the bash -c wrapper running the pgrep CONTAINS that literal string, so the loop killed its own shell (cogito-guard blocks the pkill form for this reason, but the pgrep form has the identical trap) -> RULE: when matching processes by command line, always break the self-match with a bracket class (pgrep -af 'verify_selftes[t]') or match on PID captured earlier; never assume switching pkill->pgrep removes the self-kill hazard
- [#verify][#shell] [I:7] Ran a 34-rung verification under 'timeout 1500' with output redirected; the timeout fired and I got a bare 'Terminated' with ZERO of the 25 minutes of results -> Python block-buffers stdout when it is not a tty, so a killed run loses every line it had already printed -> RULE: any long-running python check that may be killed must run with 'python3 -u' (or PYTHONUNBUFFERED=1) and tee to a log, so partial progress survives the kill.
- [#verify][#process] [I:8] A per-rung verification gate ran 25 min and printed nothing for one rung; I assumed 'slow' and kept waiting -> the generator's rejection sampler for that difficulty had an EMPTY acceptance set (0 of 80 finite parameter combinations passed), so it looped forever - the hang was in problem GENERATION, not in answer checking -> RULE: when a bounded-parameter rejection sampler (while True: draw from fixed lists; if predicate: break) is used, enumerate the full cross-product once and assert the acceptance set is non-empty; and when a stage prints nothing, bisect generation vs checking before attributing it to slowness.
- [#critical][#config][#verify] [I:9] Raised a worker's internal timeout 3600->7200 and silently wedged the whole autonomy loop -> the SAME invariant lived in two unlinked files: hermes_autobuild.py's DISPATCH_TIMEOUT and the systemd unit's TimeoutStartSec=3900. systemd won the race, SIGTERMed the cycle before any logging, and the service stuck in 'deactivating (final-sigterm)' — which leaves an OnUnitActiveSec timer with 'Trigger: n/a', i.e. NO next fire, so the loop dies silently rather than failing loudly -> RULE: when you change a timeout, grep for every OTHER layer that also bounds it (systemd unit, wrapper, client, proxy) — a comment saying 'must be greater than X' is not a link. Enforce the invariant in CODE at startup, and after any timer-unit change confirm 'systemctl list-timers' shows a real NEXT, because active+enabled does NOT mean scheduled.
- [#verify][#sector:gradus] [I:7] A 33/33 packet audit proves nothing on its own — a checker that always returns True passes identically -> the audit's own sensitivity was never measured -> RULE: after any answer-key checker, run a MUTATION pass (corrupt every key, demand 100% caught) and measure each independent path SEPARATELY; a 100k-trial simulation must be compared to the CLAIMED answer, not to your own enumeration, or it only validates your model and not the key.
- [#sector:game] [#deploy][#verify] [I:8] Task premise said the live site served a 10-day-old build and that pushing master would deploy it -> BOTH were false: production had already been updated by a local Vercel CLI deploy (.vercel/ is gitignored so git showed no trace), and the git push produced only a PREVIEW deploy because the project's production branch is not master -> RULE: before crediting a push with a deploy, diff the LIVE bytes against local HEAD (md5 + a new-only string absent from the old build) AND read the deployments API for the environment field; a CLI-deployable project can have production ahead of git, and a successful deploy status can be Preview, not Production.
- [#verify][#sector:pharmakon] [I:8] pharmakon 'extraction' rung hung forever at 114% CPU and the whole 34-rung gate stalled behind it -> ROOT: a rejection-sampling 'while True' redraw loop guarded distinctness across FOUR values, but two of them (e_scaled = E_new/E_old and cl_ratio = CL_H_new/CL_H_old) are ALGEBRAICALLY IDENTICAL when flow is unchanged (CL_H = Q*E_H), so 0 of the 80 possible draws could ever satisfy the guard — an unsatisfiable predicate, not bad luck; only 3 of the 4 were ever printed as distractors -> RULE: any 'while True: redraw until guard passes' MUST be proven satisfiable by enumerating its finite draw space once (count passing combos) — and never guard a value that is a derived identity of another guarded value. Run per-item gates with a per-item timeout so one unsatisfiable loop surfaces as TIMEOUT instead of hiding as 'still running'.
- [#config] [I:6] Asked to set effort to 'ultra' and show reasoning -> there is no 'ultra' level and the settings schema names differ from the SDK: persisted effortLevel enum is low|medium|high|xhigh (CLI --effort ALSO accepts max, the flag and the setting disagree), thinking display is showThinkingSummaries not thinkingDisplay, and 'ultracode' is a separate session-scoped boolean -> RULE: never probe settings keys by writing them and watching for a warning (validation is silent on unknown keys via --settings); write the key into ~/.claude/settings.json with Edit and let the post-edit schema validator print the authoritative field list.
- [#verify][#process] [I:8] Audited the Hermes agent layer and found hermes-letter.service failing with FileNotFoundError on send-queue.jsonl -> NOT a letter bug: send.py::_drain_queue guards os.path.exists at entry but calls _post() (slow network I/O) before os.remove(QUEUE) at the end, so hermes-drain.timer (every 15min) can drain and delete the same queue inside that window; the loser crashes, and because _drain_queue runs at the TOP of send(), the caller's OWN message is dropped before it is ever posted — 4 confirmed crashes across 8 importing agents (curiosity, daily_briefing, cogstack_reminder, hermes_letter, research_watcher, memory_lint, umbrella_research, wc_alert) -> RULE: when several timers share one queue FILE as their IPC, every check-then-act on that file is a TOCTOU race; guard the act (try/except FileNotFoundError), not just the check. A failing unit that names a shared helper in its traceback indicts the HELPER, not the unit — grep for other importers before filing it as a one-off.
- [#sector:infra] [#git][#process] [I:6] Mechanizing a prose rule (faceless git identity) tempted a hard deny like the other guard rules -> but a trivially-fixable, false-positive-prone, correctable rule should not brick a commit -> use the PreToolUse 'ask' (warn+let-proceed) primitive, not 'deny'; fail-open on every undeterminable axis. deny is for catastrophic/irreversible; ask is for 'you probably forgot, here's the fix'.
- [#sector:infra] [#critical][#config][#memory] [I:9] The Cogito loader warned 'critical set exceeds cap' for weeks and I read it as 'slightly over budget, consolidate a bit' -> simulating the loader proved only 4 of 14 criticals actually loaded: it selects on [I:9-10] OR [#critical], but 12 of 14 lessons sat at I:9-10, so after 'sort -rn' on the severity digit the survivors were decided by GNU sort's arbitrary full-line tiebreak — it kept a 1009B web-only CSS lesson (42% of budget) and CUT every boundary rule (git identity leak, main-push consent, secrets-in-flight, SKILL.md supply-chain) -> RULE: never let one field mean both 'how bad is this' and 'must it ride every turn' — severity is a property of the failure, always-loading is a property of RECOGNISABILITY (does the agent get a cue to go look it up?). Use an explicit [#always] flag decoupled from [I:], and when a budget warning fires, SIMULATE the selector and print what actually survives — an overflow warning tells you the size, never which items got dropped.
- [#sector:infra] [#verify][#process] [I:8] The consolidation conservation gate FAILED on 11 correctly-preserved playbook bullets, and I nearly filed it as real data loss -> it compared against 'git diff BASE -- ARCHIVE | grep ^+-', i.e. only what THIS change added to the archive, so anything archived in an EARLIER commit read as unaccounted; it also treated an in-place edit (adding a tag to a lesson) as delete+add -> RULE: a conservation check must test PRESENCE IN THE CURRENT FILE, not presence in the diff, and must normalise away markers (tags, counters) before deciding a line vanished. A gate that fails when nothing is wrong is worse than no gate — it trains you to click past it. After fixing any gate, POISON it: delete one real line unarchived and require FAIL, then restore and require PASS.
- [#sector:infra] [#config][#verify] [I:7] Added an [#always] selector to the Cogito loader, then the very lesson DOCUMENTING the flag matched it and re-broke the budget (+1038B) -> the selector was '^- .*\[#always\]', so any lesson whose PROSE mentions the tag selects itself; the original author had guarded #critical against exactly this and I did not carry the guard over -> RULE: a tag selector must anchor to the LEADING tag block ('^- \[#tag\]'), never '.*tag' — self-documenting memory systems WILL contain lessons that quote their own tags, so the corpus is adversarial to loose matching by construction. Verify by asserting the meta-lesson is absent from the loaded set, not just by re-reading the byte total.
- [#sector:infra] [#critical][#memory][#verify] [I:9] I decoupled always-loading from severity ([#always] flag), shipped it, then EMPIRICALLY tested retrieval and got 6/6 misses -> the demotion opened a hole nobody predicted: cogito-recall.sh skipped any lesson matching [#critical]/[I:9-10] on the assumption 'already always-loaded', which was true under the OLD selector; after the change the demoted-but-severe lessons kept #critical, lost their loader slot, AND were skipped by recall — excluded from BOTH paths, so 'demote' silently meant DELETE -> RULE: when you change WHICH set a loader selects, grep every other consumer that filters on the OLD predicate — a selector change is an interface change, and the second consumer fails silently by omission (nothing errors, content just stops appearing). Never accept 'it is still retrievable' as a design assumption: TEST it by firing the real scenario and asserting the specific lesson text appears, with a negative control.
