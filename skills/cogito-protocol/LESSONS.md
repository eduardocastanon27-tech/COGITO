# Cogito — Lessons Ledger

The durable memory of the system. Each line is one earned lesson in the form:

    SYMPTOM -> ROOT CAUSE -> RULE

An entry is warranted whenever (skill §4b): the user corrects Claude; a bug's
root cause sits at a different layer than its symptom; a help request reveals a
missing tool/permission/context; or the same friction happens twice. Capture
prospectively — the moment it happens, not at the end. This file is the
write-path that makes the protocol compound; an empty ledger means the system
is relearning its scars every session.

> Verify on every session start that this file is reachable and survives a
> write (ephemeral containers silently revert). If it does not, say so
> immediately rather than shipping a ledger that looks alive but isn't.

**Format (from 2026-06-15):** prefer `[#tag] [I:1-10] SYMPTOM -> ROOT CAUSE -> RULE` so lessons can be retrieved by tag and ranked by importance; `#critical` or I ≥ 9 always load. When this list passes ~60 entries or after a severe lesson, run a consolidation pass (merge/supersede into fewer rules, archive raw lines with provenance, never delete). Rationale: docs/projects/06-cogito-upgrade-roadmap.md.

---

## Lessons

- [#memory][#process] [I:7] Prose describes, hooks execute; "online" = durable + auto-loading (consolidates 2 prior merged rules) -> skill/CLAUDE.md text executes nothing, and memory living only in session files dies with the session -> wire the SessionStart hook + install the skill globally on day one, commit memory to a repo the user owns, and verify the ledger survives a write by re-reading it. {merged:2026-07-07}
- [#critical][#git] [I:9] Faceless (x4 raw) -> defaults leak his handle/email -> local git config Cogito / 291881939+COGITO-SUM-cloude@users.noreply.github.com BEFORE the first history op in every cogito repo; scrub his handle from ALL public output; accept 'Unverified'.
- [#deploy][#verify] [I:7] Ship-truth (consolidates 5 raw, one a prior merge) -> deploy configs are intentions, echoed OKs lie (a dirty tree aborted a 'successful' merge), a fingerprint string that predates the change discriminates nothing, and deploy MCP tools only return CLI commands -> BEFORE: verify the deploy mechanism + auth + CLI/token exist; prefer git-based continuous deploy. AFTER: verify origin/main actually moved (git rev-parse) AND live content carries a token introduced ONLY by this change (confirmed absent from the old build). Vercel protection/deletion = dashboard-only (MCP is read/deploy-only; the private hub is the Vercel 'skills' project). {merged:2026-07-07}
- [#critical][#comms] [I:9] Eduardo: non-technical, NOT a child -> adult-to-adult: plain words, short replies, analogy not jargon, do the tech myself; NO emojis/cheerleading/'baby steps' -- 'simpler' = less jargon, not talking down; re-read when a task feels complex.
- [#process][#teaching] [I:5] Teaching goals (consolidates 2 raw) -> the human is a learner too, and the adult-tuned skill does NOT transfer to a child -> treat user-growth as first-class (small, plain-language teaching woven into sessions); teaching his 5th-grade brother is NEW gently-scaffolded, decoding-first work that protects his dignity -- never reuse the adult register on a child. {merged:2026-07-07}
- [#critical][#boundary] [I:10] Classifier denial (x3 raw) -> a block is a boundary, not an obstacle -> STOP: never reroute via files/env/sed/indirection, never infer consent from an indirect remark; present the exact change + rationale and get an explicit yes.
- [#web][#nextjs] [I:7] Installed-version truth (consolidates 2 raw) -> framework APIs drift from training data (this CMS runs a modified Next 16: async params, uncached route handlers) -> read node_modules/<framework>/dist/docs for the INSTALLED version and heed AGENTS.md deprecation notes before writing framework code. {merged:2026-07-07}
- [#web][#proj:idkwhatimdoing] [I:4] BIHO CMS conventions (consolidates 2 raw) -> the React compiler forbids impure calls (Date.now/random) during render, react-hooks/set-state-in-effect is an ERROR here, and themeable art rides one config -> compute time/random values in effects/handlers; inline-disable a legit effect-sync with a one-line reason; control every backdrop from src/lib/art.ts. {merged:2026-07-07}
- [#css][#web] [I:4] CSS layering/cascade scars (consolidates 5 raw) -> perceived brightness = image_opacity x (1 - scrim_alpha); non-positioned content paints above a z-0 backdrop; UNLAYERED globals beat Tailwind @layer utilities; tall cells shove markers off a shared axis; @theme palettes add utilities without removing defaults -> tune image opacity + scrim alpha together; wrap real content in relative z-10; put base element styles in @layer base and check cascade origin (not just specificity) when a DOM-present utility won't apply; pin shared axes and hang variable content off ONE side. {merged:2026-07-07}
- [#web][#process] [I:4] User assets into the repo (consolidates 2 raw) -> chat-attached images are shown to the model but never written to the filesystem, and GitHub web upload can add a leading space to a filename -> have the user add the file via GitHub upload, then verify the exact filename (git mv stray spaces) before wiring paths. {merged:2026-07-07}
- [#critical][#verify] [I:9] 'Live/working' = a stranger's experience (x3 raw) -> deploy state, green builds (packagers only assemble), logs, and owner-session views are proxies -> verify by a logged-out load plus a rendered screenshot or playthrough before claiming it.
- [#network][#git][#boundary] [I:7] Layered gates impersonate app errors (consolidates 2 raw, one a prior 3-scar merge) -> egress allowlists, git-proxy policy, PAT scopes, 2SV, and creator-tier checks all surface as generic failures that look like the target's fault, and clearing one gate only reveals the next -> diagnose the LAYER first (transport vs policy vs logic), change ONE variable at a time, read the EXACT error string; deterministic rejections are never retried (solo/unverified Roblox creators: publish PRIVATE). {merged:2026-07-07}
- [#web][#network][#proj:paint] [I:5] Paint app env (consolidates 2 raw) -> onnxruntime-node's postinstall needs off-allowlist egress (npm rolls the whole install back), and blob-host image OPTIMIZATION is not auto-allowed while the blob host is egress-blocked locally -> npm install --ignore-scripts for design QA (ML/visualizer routes excluded); add the blob host to next.config images.remotePatterns; diagnose by fetching the page's EXACT /_next/image URL -- local blank photos = egress, not a prod bug. {merged:2026-07-07}
- [#shell][#process] [I:6] Bare Bash commands sometimes ran from /home/user, and "Shell cwd was reset to /home/user" appeared after cd commands -> this harness's per-call cwd is not reliably the project dir -> always prefix Bash with an absolute `cd /home/user/<repo> &&` (or use absolute paths / git -C); never rely on inherited cwd.
- [#eyes][#network] [I:6] Give it eyes -- never debug blind (consolidates 3 raw, two prior merges) -> locked-down envs (egress policy, TLS interception, ephemeral disks) and visually unreachable runtimes (container-only URLs, devtools-less phones) make guessing burn turns -> FIRST ask the user to open the env's network policy; else install registry-served Chromium (npx @puppeteer/browsers install chrome@stable; --no-sandbox --ignore-certificate-errors; idempotent ensure-eyes.sh), have the user send a screenshot, or ship an on-screen debug status box. {merged:2026-07-07}
- [#eyes][#verify] [I:7] Pixels are the visual truth (consolidates 3 raw, one a prior merge) -> markup audits, byte counts, green captures, and 'the code runs' all miss rendered reality (cert walls, favicon 404s, builder sites misjudged both ways) -> visual 'done'/triage = render and READ the pixels back, curl the page's exact asset URLs, demand a CLEAN console, from a logged-out context. {merged:2026-07-07}
- [#git][#process] [I:7] A stale sync step pushed to the OLD default branch by name after the default moved to main, resurrecting the branch the user had just deleted -> any push to a deleted remote branch silently re-creates it -> sync ONLY main + the active working branch; never hardcode an old default in a sync step. {demoted from #critical 2026-07-07}
- [#process][#agents] [I:6] Sub-agent economics (consolidates 2 raw) -> unbounded recursive fan-out wastes rate limits (30 overlapping researchers), and background agents that only write output at the end lose everything when the session dies -> cap fan-out at 3-5 well-scoped agents with disjoint topics, and have long-running agents write partial results to disk incrementally. {merged:2026-07-07}
- [#shell][#verify] [I:7] Ledger-surgery traps (consolidates 3 raw) -> an Edit old_string starting with a newline welds neighbour lines while content greps still pass; '-'-leading data parses as grep flags; grep -q at a pipeline's end under pipefail SIGPIPEs its feeder (random false-fails) -> delete lines by matching the TRAILING newline; end grep option parsing with -- and feed grep -q via herestring/file; gate structural edits on line COUNT + git diff; test a gate's PASS and FAIL paths -- a failure set that changes between identical runs is the gate's own bug. {merged:2026-07-07}
- [#git][#verify] [I:7] Destructive ops vs uncommitted work (consolidates 2 raw) -> git checkout -- <file> and reset --hard eat uncommitted edits sharing the tree (a self-test nearly wiped its own deliverable) -> commit or stash work-in-progress BEFORE any destructive op or self-test; undo a test change with a targeted revert (git stash -p / re-grade), never a file-level checkout on a file holding mixed edits. {merged:2026-07-07}
- [#shell][#verify] [I:6] My PreToolUse guard blocked its own git commit because the commit MESSAGE mentioned the commands it blocks (pkill -f, rm -rf /) -> I normalized by stripping quote CHARACTERS (tr -d), which merged quoted argument text into the command and defeated the mention-vs-execution check -> a command-guard must remove quoted SPANS (not chars) so in-string mentions vanish, and its selftest must include a commit/echo that mentions the very commands it blocks.
- [#council] [I:6] A council is only as good as its inputs (consolidates 2 raw) -> panelists reason about the system-in-the-abstract, and a confident verdict flips when the user supplies real constraints (a DEFER correctly REVERSED on three new factors) -> surface the user's full goal + hard constraints BEFORE convening, give the judge those constraints + repo access to check every proposal against them, and treat the verdict as advice the user can override, never a ruling. {merged:2026-07-03}
- [#memory][#process] [I:8] Always-load hygiene (consolidates 2 raw) -> a growing always-loaded ledger silently scales per-turn cost (measured: 6.2k tokens/turn, 83% the ledger), and a correction that merely coexists with what it overturns leaves the RETIRED rule loading every turn -> keep the always-load set to #critical + a tag index (measure with scripts/cogito-budget.sh), and when a lesson corrects another, consolidate or archive the old one in the SAME edit. {merged:2026-07-07}
- [#critical][#memory] [I:9] Brain copies diverge (x2 raw) -> a wrong-direction copy is silent brain damage (blind cp deleted 14 lessons) -> load from ONE canonical ref; sync = diff BOTH ways, merge to the superset, write the same result to both; unexpected deletions in a diffstat = stop.
- [#critical][#git][#boundary] [I:9] Main needs a main-SPECIFIC yes (x3 raw) -> a generic go/save/checkpoint is NOT consent -> never bundle a main push into a chained command; docs stay on the branch; a new instruction never overrides a standing boundary: surface it.
- [#process][#verify] [I:6] A "next on the roadmap" item (#4 verify-before-store) was already built in a prior session — the skill-check gate script, the documented rule, and the lesson-probation buffer all existed via an earlier merge — and I nearly rebuilt it -> a ranked/ROI roadmap lists what's VALUABLE, not what's undone, so "ranked next" does NOT mean "not yet done" -> before building any roadmap or upgrade item, grep the repo for an existing implementation first and run it; confirm-and-verify beats rebuild.
- [#council][#process] [I:8] A 5-researcher council picked the 3D engine on standalone merits while the avatar+voice+lip-sync linchpin only exists for Three.js -> an isolated panelist cannot see a cross-domain linchpin -> choose the substrate where the HARDEST integration seam is already solved for free; in synthesis, cross-check every panelist's pick against the OTHER panelists' linchpins. {demoted from I:10 2026-07-07}
- [#shell][#process] [I:8] Harness process control (consolidates 2 raw) -> pkill -f matches its own command line (killed the shell, exit 144) and foreground sleep is blocked -> stop processes by explicit PID (pgrep then kill "$PID"; enforced by scripts/cogito-guard.sh), and wait for a fresh server with curl --retry N --retry-connrefused --retry-delay 1. {merged:2026-07-07}
- [#verify][#process] [I:6] Verifying the teacher's write-back against the REAL learning log graded a review the user never actually did (L3 box 1->2), polluting the genuine spaced-repetition schedule -> testing a STATE-MUTATING feature against the live data store writes test artifacts into real records, and "grade only on genuine recall" makes that a correctness bug, not mere clutter -> test state-mutating features against a throwaway/sandbox subject, or revert the artifact immediately; never let a self-test count as real learner data.
- [#verify][#process] [I:6] Silently dead features (consolidates 2 raw) -> a try/except downgrading to a fallback (audio=False) and a first-token parse of a verbose LLM verdict both shipped no-ops that looked alive -> unit-verify the inner op (buffer lengths, return sizes) before shipping; parse LLM verdicts by SCANNING for expected tokens (negative/skip first), tested against a REAL model reply. {merged:2026-07-07}
- [#web][#verify] [I:6] Loaded 3D model invisible/off-screen (consolidates 2 raw) -> skinned/bind-pose bounds are invalid right after load (Box3 framing lost the model), and metallic PBR is black without an environment map -> never Box3-frame a skinned glTF on load (fixed camera; confirm it renders bare first); add scene.environment (PMREMGenerator + RoomEnvironment) and normalize materials before blaming the file; keep a visible fallback. {merged:2026-07-07}
- [#network][#council] [I:6] OpenRouter realities (consolidates 2 raw, one a prior merge) -> free slugs rotate to paid (404) or throttle (429), silent fallbacks hide WHICH model served, and usage leaderboards rank an app's cheap-token VOLUME, not model quality -> use a fallback chain (openrouter/free leads), query /models for what is currently free, print the serving model on stderr, and never read a usage rank as a quality signal. {merged:2026-07-07}
- [#critical][#boundary] [I:10] Never arm an agent with a wallet -> autonomous money-handling collides with the no-autonomous-spending guardrail ($3 cannot cover fees) -> agents do $0 faceless labor; a HUMAN holds the money rail and presses send.
- [#council][#verify] [I:7] Council decorrelation (consolidates 3 raw — same-base "consensus" x2, the never-firing non-Claude voice) -> same-base-model panelists converge on the house thesis and read as triangulation while being ONE voice in triplicate; the decorrelated voice was opt-in prose that never auto-fired, and docs drifted from code -> decorrelate panels across MODELS (wire the non-Claude voice to fire EVERY run and feed the judge), weight grounded dissent + a clean repo-grep over confident agreement, and settle remaining forks with a live test, not more same-model opinion. {merged:2026-07-03}
- [#shell][#verify] [I:6] the converge hook printed nothing in a by-hand dry-run -> its repo-root fallback used a hardcoded dirname/../.. depth that lands one level ABOVE the repo when CLAUDE_PROJECT_DIR is unset, so it found no git and silently exit 0 (masked in production only because CLAUDE_PROJECT_DIR is set at Stop) -> never derive a script repo-root by hardcoded ../.. depth; use git -C "$(dirname "$0")" rev-parse --show-toplevel, and test hooks with the env var BOTH set and unset.
- [#git][#verify] [I:6] Converge internals, CURRENT state (consolidates 3 raw — the per-file blob fix, the Stop-hook race, the read-the-script correction) -> scripts/cogito-converge.sh now builds a SYNTHETIC commit = main's tree + only the brain blobs (per-file HEAD-vs-main diff, so appends AND hand-edits converge, and withheld CODE commits CANNOT ride along — the old whole-HEAD fear is fixed) -> trust the synthetic converge for brain files, keep converge ordered before git-check so the nag doesn't race it, and when a stale scar contradicts current code, READ the script before acting on the fear. {merged:2026-07-03}
- [#memory][#boundary][#verify] [I:7] Satellite wiring truth (consolidates 2 raw) -> sessions are per-repo silos, a satellite 'verified' only from the hub had a stale local-only install, and a wired path is unproven until a lesson lands from the actual agent -> a satellite counts as wired only with committed sync+learn scripts + SessionStart hook AND a lesson observed arriving FROM it (Hermes: confirmed 2026-07-04 via cogito-learn.sh). {merged:2026-07-07}
- [#critical][#security] [I:9] Secrets in flight (x2 raw) -> never fetch-pipe-run secret-touching code (run committed, reviewed scripts) and never disable TLS verification with a secret in flight -- fail loudly and install the egress CA instead.
- [#security][#boundary][#memory] [I:8] Brain write-back architecture, SETTLED (consolidates 4 raw council/live-probe scars) -> a frictionless cross-repo token-write is exactly the exfil shape safety systems block; no agent session can safely bridge a PRIVATE satellite to the hub; and correcting canon cannot be both fully autonomous and safe for a solo/$0/faceless owner -> satellites PROPOSE, never push: lessons land in the satellite's OWN committed cogito-outbox.md; the hub drains/gates them (append = cheap + auto-merge, contradiction = one-tap human circuit-breaker); hub-write credentials NEVER sit in agent sessions or satellite silos; the cost of overturning a belief scales with the blast radius of believing it. {merged:2026-07-03}
- [#critical][#security] [I:9] Auto-syncing SKILL.md from a remote tip = supply-chain backdoor -> one tampered 'lesson' reprograms every future session -> sync LESSONS.md ONLY, never auto-overwrite SKILL.md; pin to a reviewed SHA; keep the protocol local + reviewed.
- [#config][#shell] [I:6] Committed is not active (consolidates 2 raw) -> a hand-merged settings.json with two top-level {} silently never parses, and mode-644 scripts behind [ -x ] fail-open guards are invisible dead code -> validate JSON after every manual merge (python3 -m json.tool), chmod +x scripts at creation, and prove each guard-protected step actually FIRES once. {merged:2026-07-07}
- [#verify][#process] [I:8] Scanner/audit claims are hypotheses (consolidates 3 raw) -> auditors grep source/snippets and miss data-driven rendering, valid-but-odd CDN keys, and live processes (a 'prunable' node_modules hosted the LIVE WhatsApp bridge) -> before acting -- especially irreversibly -- re-verify each specific claim grounded: curl the URL and the rendered HTML, cmp/diff -rq, unzip -l, pgrep -af. {merged:2026-07-07}
- [#process][#boundary] [I:6] Believed a Cogito web session could never reach a sibling repo because the add_repo/list_repos MCP tools won't load -> those tools are internal/undocumented; the real user-facing mechanism is the "+" repository selector chosen when the web session is STARTED (a session can use ANY repo the connected GitHub account can see, private included) -> to work across repos start the session with all needed repos via "+"; you cannot bolt a repo onto a running session, so plan multi-repo work at launch.
- [#shell][#bug] [I:7] a localhost control-room 404'd on its own page and POST handlers returned an empty body, while env-overridden endpoints worked -> ROOT was built as `$(git ... || cd ... && pwd)`, which parses as `(git||cd) && pwd` and on git-success appended pwd too, making ROOT a two-line string that corrupted every NON-overridden derived path -> a shell fallback ending in `&& pwd` must never be chained after `||` on one line; set the primary, then `[ -n "$x" ] || x="$(fallback)"`.
- [#process][#architecture] [I:7] porting the file-native control room (a local server writing queue.json) into the deployed CMS could not keep its file-based review state -> Vercel/serverless has an ephemeral, read-only filesystem, so the "source of truth" for the same feature differs by runtime -> when porting a file-native cogito tool to the serverless CMS, persist state in the app's DB (a Mongo model), never a queue file; and keep the dedupe key byte-identical across both so the two implementations agree on "already in the brain".
- [#process][#assumption] [I:8] built a full macOS .app launcher AND ran a packaging council framed "macOS-primary" before the user said "i have linux not mac" -> I inferred the OS from ambient cues (a browser screenshot, Vercel) and carried the unconfirmed assumption straight into OS-specific build work + a whole council round -> before building or deliberating ANYTHING OS-specific (packaging, app bundles, signing, launchers), confirm the target OS explicitly; never infer it from a screenshot or context.
- [#verify][#types][#process] [I:8] my Mongoose->NeDB offline adapter compiled + passed a 22-case unit test, but a live test on the owner machine found it crashed offline on Project.exists()/Site.exists() — a method my hand-listed query inventory missed -> a compatibility shim CAST to the original type (`as Model<T>`) makes the compiler TRUST it has the full API, so missing methods are invisible to typecheck and only surface at runtime -> when shimming an API behind a cast: inventory the original calls EXHAUSTIVELY (grep every `.<method>(` on the models, never a hand-picked subset) AND treat a live/runtime test as mandatory, because the build cannot catch a method the cast hides.
- [#process] [I:8] Roblox Studio wouldn't run on a 2.78GB Chromebook, blocking the whole game on hardware -> the game was tied to ONE engine the machine can't run, treating the platform as fixed -> when the PLATFORM is the blocker, port the DESIGN (configs/mechanics/numbers) to a stack the user CAN run (here a Three.js browser build reusing every config value) — reuse the design, not the engine code, and ship something playable today. {via:rescue claude/lessons-session-update 2026-07-02}
- [#git][#verify] [I:7] 'Unmerged branch' rescue (consolidates 3 raw) -> branch ancestry lies after squash-merges, git cherry only proves a commit wasn't replayed, and stale origin/* tracking refs show ghosts GitHub already deleted -> git fetch --prune + git ls-remote --heads origin for the LIVE remote, then diff the branch FILES against the target (md5 per file + git diff target..branch) -- the file-level diff is the only content-truth. {merged:2026-07-07}
- [#deploy][#process][#verify] [I:6] a fresh systemd timer's first test-fire "failed" by timeout+SIGKILL even though the journal showed the deploy succeeded (Vercel CLI hangs after success without a TTY) -> a unit whose success tracks a CLI exit code inherits every hang/quirk of that CLI -> define oneshot success as the OBSERVED outcome (wrapper script: internal `timeout` well under TimeoutStartSec, then curl the live artifact and grep for today's stamp), and always test-fire a new unit once before trusting its schedule. {via:session 2026-07-02 house-cleaning}
- [#deploy][#process] [I:8] [d:2026-07-03] ACTIVE-MISSION carried "owner intends to delete the CMS Vercel deploy (laptop-only)" as harmless, but the paint client site's estimate form POSTs to that deploy's public endpoint — deleting it would have silently killed lead capture for a live business site -> a public endpoint is a production dependency; docs recorded the intent without enumerating consumers -> before deleting/retiring any deployment, enumerate its inbound consumers (grep sibling repos for its URL/env refs, check API routes), and re-verify any stale "owner intends to delete X" note against X's CURRENT consumers before acting on it. {via:session 2026-07-03 paint setup}
- [#verify][#process] [I:6] [d:2026-07-03] paint leads sat in the CMS for 3 weeks with notifyEmail="" — the pipeline "worked" (docs persisted) but no human was ever notified; luckily all 8 were tests/spam -> a capture pipeline is not DONE until the notification leg is verified received by a human; persistence alone is a silent-failure mode -> when wiring any lead/alert flow, the definition-of-done is the human-received message (inbox check), not the stored record. {via:session 2026-07-03 paint setup}
- [#git][#deploy] [I:6] Assumed a pushed claude/* branch would auto-merge to main within seconds (per prior note); it did NOT — no PR, file absent from default branch after the window -> the converge/auto-merge hook only carries COGITO brain/lesson files to main, not arbitrary code changes like a devcontainer -> for normal code, land it deliberately via gh pr create + gh pr merge and VERIFY by fetching the file from the default branch (gh api contents), never assume auto-merge fired.
- [#deploy][#proj:cogstack] [I:6] cogstack prod deploys (absorbs playbook P014) -> the Vercel project has NO git auto-deploy (a merge shipped nothing for 26h); CLI auth persists at ~/.local/share/com.vercel.cli/auth.json (actor=claude-code) -> every prod deploy = 'npx -y vercel@latest deploy --prod --yes' from the linked repo, no global CLI needed; verify with logged-out curl + content grep. {merged:2026-07-07}
- [#deploy][#process] [I:8] Prod deploy shipped OLD code though the commit was green -> a parallel session checked out its own branch in the SAME repo dir between my commit and vercel's file upload, so the CLI snapshotted foreign tree state -> when parallel sessions share one checkout, deploy from a dedicated git worktree pinned to your commit, and trust only live content grep, never deploy exit status
- [#git][#shell] [I:4] git rm -r android ios removed 73 tracked files but left 32 ignored files on disk -> Capacitor scaffolds carry nested .gitignore files, so bundled/generated copies were untracked and git rm never touched them -> when purging a whole scaffold dir, follow git rm with rm -rf <dir> (or git clean) and re-grep the working tree, since git rm only removes TRACKED files.
- [#memory][#verify] [I:6] Status notes and mtimes go stale (consolidates 2 raw + playbook P015) -> memory notes record write-time state, and a file's mtime dates its LAST write, not the change you care about -> never state arm/live/deploy status from a note: grep the actual settings/hook/port first; date a change by diffing a prior version or backup; periodically batch-verify status-bearing note lines (logged-out curl per URL + disk/port checks), fixing only what fails. {merged:2026-07-07}
- [#shell][#verify] [I:6] A PROGRESS.md stamp-bump silently kept the OLD date while the script exited 0 and still committed — a sibling append had dirtied the file, masking the no-op -> BEGIN{IGNORECASE=1} is a gawk-only extension and this box's awk is mawk (Debian default), so the case-insensitive match never fired -> never use gawk-only awk (IGNORECASE/gensub/asort) in cogito scripts; use explicit [Ll] char classes, and verify a text transform by READING THE FILE BACK, never by exit-0 or 'it committed'.
- [#git][#deploy] [I:6] cogito-guard blocked a legitimate 'git push origin main' even with the user's explicit 'land it to main' -> the guard gates ALL direct main pushes BY DESIGN (a deliberate-confirmation speed-bump that documents its own override), NOT a security classifier -> on an EXPLICIT main-specific yes, push with 'COGITO_ALLOW_MAIN_PUSH=1 git push origin main' (the guard's sanctioned escape hatch); this is the required deliberate confirmation, distinct from tunneling around an external security denial (the critical token-file scar) — never use the override without a fresh main-specific yes. {edited 2026-07-07: unbracketed a tag mention that force-loaded this line}
- [#process][#verify] [I:5] Eduardo thought closing the terminal turns Hermes off and couldn't reach it on WhatsApp -> Hermes runs as LINGERING systemd user services (hermes-gateway + hermes-claude-proxy, Linger=yes) so it survives the terminal WINDOW closing; the WhatsApp 'outage' was the bridge getting OOM-killed on the 2.78GB no-swap Crostini box (signature in journal: '[Errno 107] Transport endpoint is not connected: /proc/stat') -> to reach Hermes while gaming, just close the terminal window (never 'Shut down Linux'); guest swapon is blocked in Crostini ('Operation not permitted'), so harden by freeing RAM / host-side ChromeOS swap, not a guest swapfile. Verify link with python3 ~/.hermes/scripts/send.py and GET :3000/health.
- [#web][#verify] darwin-web live showed 'booting…' forever, dead controls, no viz -> a top-level 'let curSound = randSound()' at init closed over 'const SCALES/WAVES' declared 140 lines LATER = a runtime TDZ ReferenceError that killed the whole IIFE; node --check passed (valid syntax, not runtime) and there's no browser on this box to catch it -> for a browser page built headlessly, VERIFY INIT by executing the inline <script> in a minimal DOM shim (window/document/rAF/localStorage stubs) and run one frame; declare consts before any init-time call that closes over them.
- [#web][#verify] [I:8] web-build-loop swore 'truth = rendered pixels' but Fable5 audit + my checks proved every verify path was fiction: design-qa subagent absent (~/.claude/agents empty), see_site.sh core-dumps headless on Crostini, site_audit.py scores a 'Create Next App' site 100/100 -> we shipped a verification DOCTRINE without ever running its tools end-to-end -> a named verify path counts ONLY after you've run it and seen a TRUE signal; audit your own eyes before trusting them. Wire PageSpeed Insights API (keyless, cloud Lighthouse) + Firecrawl screenshots as the real backbone on a 2.78GB box.
- [#web][#boundary] [I:8] rubric pays points for 'trust signals (reviews/ratings/user counts)' and the model duly invented them: storescript ships fabricated '200+ independent sellers', jlpainting 'best painter in Columbia' -> rewarding proof without an integrity cap is textbook reward-hacking and violates hard-science-rigor -> cap any unverifiable/invented claim at <=49 total; proof must EXIST before it's cited; when there's none, the honest constraint IS the copy.
- [#process][#verify] [I:6] Told Eduardo I extracted master-tier web skills 'from Fable 5' but parenthetically attributed it to Claude 3.5 Sonnet -> stale/wrong model attribution; I run on Opus 4.8 and CAN dispatch a Fable 5 subagent (model:fable), so the plan is real but the label was fabricated -> never hand-label which model produced an artifact from memory; state the model you actually ran and verify reachability before claiming it.
- [#process][#boundary][#deploy] [I:7] Fable-5 xhigh web-skill workflow kept dying then hit 'monthly spend limit' -> TWO stacked constraints in the Hermes/WhatsApp model: (a) each inbound message is a short-lived claude -p process so BACKGROUND workflows get killed at turn-end and never finish long agents; (b) 5+ xhigh Fable-5 agents burn the Anthropic monthly spend cap fast -> for Hermes long work, run stages as SYNCHRONOUS blocking agents (hold the process open), checkpoint each agent's output to disk so a kill loses nothing, and budget Fable-xhigh fan-outs against the spend cap; a spend-limit error is a hard billing wall (only Eduardo raises it at claude.ai/settings/usage) — never retry it.
- [#boundary][#security] [I:8] User said 'wire it live' for dormant autonomous scripts -> a generic go is NOT consent to enable persistent unsandboxed 'claude -p --dangerously-skip-permissions' timer loops; the auto-mode classifier correctly blocks it -> BUILD the units + code, then hand the enable command to the user to run outside the sandbox; never route around the classifier via manual symlinks/env. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#verify][#process] [I:7] After a session died mid-run I almost trusted recent mtimes as proof the fixes applied -> a fresh mtime means the file was TOUCHED, not that the intended edit landed (adaptive-learning showed an old 07-03 mtime = never patched; that was the exact death point) -> verify fix CONTENT with grep/diff, never the timestamp. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#boundary][#process] [I:7] An F/'dead' grade tempted a bulk delete of niche scripts -> a low grade means broken/unproven, NOT unreferenced or unwanted (a bootstrap.py 'caller' was a false grep match; the F-graded cluster was actually Hermes's Eduardo-supported autonomous 'dream' loop) -> before retiring anything, grep REAL consumers AND check intent/relationship; retire only the truly orphaned, surface the rest for a decision. {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#config][#process] [I:7] Nightly journal silently ran on Fable 5 the day the interactive default changed -> an unpinned 'claude -p' in a cron/timer script inherits whatever model the user last saved with /model, and 'claude -p' can also return truncated text with exit 0 -> timer scripts always pin --model explicitly and verify the output looks FINISHED (end-check + one retry) before writing it to disk {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#bug][#web] [I:6] research_watcher surfaced noise/misses for 'A+B' PubMed terms -> urllib.request.quote() escapes '+' to %2B, which PubMed reads as a literal plus, not the AND-word-gap the terms intended (neuroplasticity+learning: 0 hits escaped vs 12 literal) -> when building a query URL, pass safe='+' (or the API's operator chars) so intended operators survive percent-encoding; verify by comparing hit counts both ways {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
- [#process][#memory] [I:6] The amem network fed the weekly letter but froze 10 days on its seed -> an accumulating store had a READER (letter's inject) but no automatic WRITER — only a manual 'amem.py add' nobody was running -> when auditing any memory/knowledge store, verify something WRITES to it on the same cadence it's read, not just that reads work; a read-only consumer of a 'growing' store is silent staleness {via:drain /home/mesmer/.claude/cogito-pending-lessons.md 2026-07-09}
