# Sector playbook: game
_Distilled top rules for game work. Loads at project open when PROGRESS.md declares `game`. Thin seed — grows as game scars are captured._

- When the PLATFORM is the blocker (an engine the machine can't run), port the DESIGN — configs, mechanics, numbers — to a stack the machine CAN run; reuse the design, not the engine code, and ship something playable today.
- Solo/unverified Roblox creators publish PRIVATE; a deterministic platform rejection (creator-tier, policy) is a boundary, not a bug — never retry it.
